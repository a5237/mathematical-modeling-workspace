# 虚拟环境说明

本目录是 Python 虚拟环境的存放位置，但虚拟环境本身不可跨电脑移植。

原因：
- 虚拟环境中的 `pyvenv.cfg` 记录了创建时 Python 解释器的绝对路径。
- `Scripts\python.exe` 等可执行文件内部包含硬编码路径。
- `Lib\site-packages\` 下的部分包包含编译后的二进制文件（.pyd），与特定操作系统、Python 版本和 CPU 架构绑定。

因此，直接复制或压缩本目录给另一台电脑无法正常运行。

正确的做法是：在本电脑上使用项目根目录的 `requirements-modeling.txt` 重新创建虚拟环境。

## 快速重建

在 PowerShell 中执行以下命令（需以管理员身份运行，或确保有写入权限）：

cd <项目根目录>
.\\.venv-modeling\\setup.bat

该脚本会自动完成：删除旧环境、创建新环境、安装依赖、运行环境检查。

## 手动重建步骤

若脚本无法执行，可按以下步骤手动操作：

1. 确认已安装 64 位 Python 3.12（推荐 3.12.10）。
2. 在项目根目录打开 PowerShell。
3. 删除本目录（如果存在）：`Remove-Item -Recurse -Force .venv-modeling`
4. 创建新环境：`py -3.12 -m venv .venv-modeling`
5. 安装依赖：`.\.venv-modeling\Scripts\python.exe -m pip install -r requirements-modeling.txt`
6. 运行检查：`.\.venv-modeling\Scripts\python.exe shared-tools\check-modeling-env.py`
7. 确认输出包含 `RESULT: PASS`。

## 注意事项

- 不要修改 `pyvenv.cfg` 来尝试适配另一台电脑。
- 不要将本目录提交到 Git 仓库（已在 `.gitignore` 中排除）。
- 如果遇到依赖安装失败，请记录完整错误信息并提供给维护者。

## 相关文件

- 依赖清单：`../requirements-modeling.txt`
- 环境检查脚本：`../shared-tools/check-modeling-env.py`
- 全局环境说明：`../数模环境说明.md`