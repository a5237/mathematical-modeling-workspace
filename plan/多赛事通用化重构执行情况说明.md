# 多赛事通用化重构 · 待办事项

> 对象方案：`plan/mathematical-modeling-workspace_多赛事通用化重构方案.md`
>
> 执行记录已归档到提交历史，本文件只保留尚未执行的事项：
>
> - `ee26a06`：通用内核改造 + CUMCM 迁入 profile（含页数口径键的两段式契约加载）；
> - `2419995`（分支 `feature/mcm-icm-profile`）：MCM/ICM profile、美赛规则基线 `OFFICIAL-MCM-ICM-001`、英文论文框架模板，以及 `body_page_minimum`/`body_page_maximum` 下沉到 profile。
> - 工作区未提交改动（T1、T2，2026-09-26）：4 篇 MCM/ICM 优秀论文（2022 B/C、2025 C/E）入库并按国赛同一格式产出预处理副本；`00-format-layout/` 按赛事分 `cumcm/` 与 `mcm-icm/`，`01`–`03` 保留为跨赛事方法类资源。
>
> 冲突判据（T4–T8 的上位原则）：**通用规范与当届官方或某一赛事要求冲突，即说明通用规范不够"通用"；下沉或删除，不加条件豁免句。**

## 待办

### T3 提交前规则重核

- 美赛正式参赛或提交前重新打开 COMAP 官方 Instructions 与 AI 政策核对当届要求，更新 `config/contests/mcm-icm/rules.md` 的 `last_verified` 并记录规则生效日期；页数口径（含不含 Summary Sheet、AI 报告是否计页）与 25 MB 上限必须逐项复核，不得只改年份。

### T4 接入第三赛事前的 Core 中性化（总则 + 逐项）

- **总则（用户定）**：通用规范与当届官方或某一赛事要求冲突，即说明该通用规范不够"通用"。修法是**把赛事属性从 Core 下沉到 profile，或直接删除**，**不得**在 Core 里写"中文论文用 A、英文论文用 B"这类条件豁免句。`docs/standards/paper-quality-audit.md` 的「国奖竞争力→获奖竞争力」替换是这条原则的既有先例。
- 下列 T5–T8 均是这条原则下的下沉/删除项，属"冲突即 Core 的问题"，不是"给美赛开口子"。

### T5 `PW-FMT-001` 排版：移出中文赛事具体值，Core 只留语言无关纪律

- 冲突证据：`docs/standards/paper-formatting.md` 里 §1.2（`:28-29` 中文字符宋体、强调加粗宋体）、§1.3（`:40-57` 小四/五号/三号、首行缩进 2 字符、段前段后 0、1.5 倍行距）、§1.4（`:61` 一级标题用"一、"、`:63` 一级标题居中）、`:18` 默认 A4、`:23` 不设装饰页眉/页码页脚居中——这些是中文赛事属性，无对应官方条款时直接压到英文论文上，而审校 Skill `SKILL.md:20` 会对任一论文执行 `PW-FMT-001`。
- 处理：具体值下沉到各 profile（承载物用 `config/contests/<profile>/rules.md` 或 `profile.yaml` 排版段，实现时定其一）；Core 仅保留与语言/纸张无关的纪律：表题在上、三线表、公式不截图、跨页表重复表头、单位与有效数字、"不得缩字号/压行距凑门禁"。Core 措辞改为"字体、字号、标题编号与对齐、纸张、页眉页码由当前 profile 规定，profile 未规定时服从官方基线，两者皆缺才回落本节通用纪律"。
- 顺带：`paper-writing.md:118`"中文名称（英文全称，英文缩写）"、`:29`/`:557`"全国组委会"、`:594`"赛区" 一并中性化。

### T6 `body_word_minimum` 随页数一并下沉 profile

- 冲突证据：页数口径已在 `2419995` 下沉，但字数仍留 Core——`paper-writing.md:10` `body_word_minimum = 5000`、`:192` 统计口径写作"问题重述→模型的评价与推广"（国赛章节名）。CUMCM 正文不限总页数、美赛整个 PDF 25 页封顶（`mcm-icm/rules.md:14,19`），两个长度约束方向相反。
- 处理：Core `PW-LEN-001` 改为"必须达到当前 profile 契约声明的字数下限，范围按该 profile 框架中承担'问题重述'与'模型评价与推广'职责的两节之间界定"；数值进两套 `rules.md`（cumcm 保持 5000；mcm-icm 数值待美赛范文校准，可只设上限不设下限），`profile.yaml:contract_keys` 各加一项；`audit_modeling_project.py:99` 改用 `getattr`（同 `:101` 页数下限写法）。
- 计数法（汉字/英文词/数字串各按 1 字）不变，已兼容英文。

### T7 引用与章节结构：Core 固定的是职责，不是国赛形状

- `PW-CITE-001`（已定）：两本指定中文教材（`paper-writing.md:548-552`）下沉为 CUMCM profile 要求；Core 只留"参考文献 ≥6 篇 + 至少 1 本教材类方法学来源，且须在正文方法介绍处实际引用"。修 `paper-writing.md:568` 与两套模板；同时补 CUMCM 模板正文缺失的 `\cite{modelbook}/\cite{algobook}`（当前 `cumcm:197,319,363` 仅引 `topic-ref-*`，`mcm:179,300,344` 同理）。
- 固定结构：Core `:127-188,196` 把"8 个一级中文章节"当固定，本身也是国赛形状。原则下同——Core 固定"章节职责与顺序"，各 profile 框架以官方语言实现。
- 待办：英文模板 `mcm…tex:385` `\section{Conclusion}`、`:373` `Sensitivity Analysis` 归位——**先核 2027 届 COMAP Instructions（`mcm-icm/rules.md:30` 官方来源）是否把 conclusion 列为 solution 组成部分**：有官方依据则按"官方要求"保留；无则并入"模型评价与推广"、敏感性分析挪进各分问结果分析，与 CUMCM 同构。不得未经核验就为迁就模板改 Core。

### T8 交付物：国赛物件不得默认套到美赛

- 冲突证据：初始化器 `init_modeling_project.py:34` 对**任何** profile 都生成标题"支撑材料文件清单"的 `08-delivery/file-list.md`，而 `mcm-icm/rules.md:23` 禁止提交单 PDF 以外的任何文件；`paper-writing.md:594,610`、`data-reproducibility.md:101`、`workspace-layout.md:107` 里的"支撑材料/交付压缩包/赛区"是国赛交付属性。
- 处理：`file-list.md` 标题与交付说明改为 profile 感知（美赛为"提交 PDF 内含清单"，或该文件仅 CUMCM 生成）；上述 Core 交付/匿名措辞中性化为"当前 profile 官方基线所要求的交付件"。

### T9 正文模板硬错与规范不合（不依赖决策，可与 T4–T8 并行先做）

- `mcm…tex:47` 用 `\pageref{LastPage}` 作 "Page x of 25" 总数，而 `:437` 的 Report on Use of AI 在 `\end{document}` 前、计入 LastPage → 页眉把不计页的 AI 节算进总页数，违反 `mcm-icm/rules.md:19,37` 与官方示例；需改为取正文末（附录/AI 节之前）的页数标签。
- `mcm…tex:437` `Report on Use of AI` 位于 `\appendix`（`:420`）后，渲染成 "Appendix B"，与官方"置于方案之后、独立附加节"不符；需脱离附录编号序列。
- 两套模板标签/路径 `eq:q1-*`、`tab:q1-*`、`../03-models/q1.py`（`cumcm:272,299,479,483`；`mcm:253,280,425,429`）违反 `naming.md:19,26,38`（应 `q01`、`eq:q01-*`）。
- `mcm…tex:135,166,418,434` 把"25 页"写死 4 处，应引用 `body_page_maximum` 键与 `OFFICIAL-MCM-ICM-001`，避免改届要改 4 行；`cumcm:413` `\renewcommand{\refname}{七、参考文献}` 写死序号（属 T7 结构下沉连带项）。

### T10 契约可执行性（口径与校验）

- 页数同键不同口径：`cumcm/rules.md:15`（正文段口径）与 `mcm-icm/rules.md:14`（全 PDF 口径）共用 `body_page_maximum`，但 `paper-quality-audit.md:146` 只说"口径由 profile 定义"，报告字段 `body_page_range/body_page_count`（`:205-208`）与 `audit_modeling_project.py:124-132` 都无承载 → 需决定：字段名与文档措辞是否改为赛事中性（如 `counted_page_range`），或明确该字段口径由当前 profile 决定并写入 `paper-quality-audit.md`。
- `control_contracts.py:209-210` 只要目录名命中即返回 profile，使 `contest: mcm-icm` 也通过，比 `naming.md:7`"必须是 `contests` 列表声明的标识"更宽 → 二者取其一（收紧内核，或在 naming.md 承认目录别名）。
- `project.yaml` 的 `profile` 字段（`init:31` 写入、`naming.md:8` 规定）无人校验（`audit_modeling_project.py:26-35` 只读 `contest`）→ 决定删除该字段或加一致性校验；两个 Skill 描述的"`profile` when present"分支代码未实现。
- 契约键字面量在 Core 散文里重复：`paper-quality-audit.md:8-9` vs `:145`（5图3表）、`paper-writing.md:10` vs `:192`（5000字，见 T6）、`pre-writing-learning.md:6` vs `:25`（≥2篇）→ 散文改引键名，不散写数值。

### T11 入口与导航事实漂移（低风险，顺手修）

- `README.md:3` 仍写"当前支持 CUMCM"（磁盘已有 cumcm+mcm-icm 两套）；`:70,148-149` 与 `docs/guides/paper-production.md:10,18,24` 只有 `--contest cumcm` 示例，而后者是命令唯一权威 → 补 mcm/icm 示例并说明 `--contest` 取 `profile.yaml` 声明值。
- `config/README.md` 未提 `contests/`；`workspace-layout.md:37-64` 仓库树缺 `config/contests/` 与 `resources/templates/contests/`（与自身 `:70` 表述不一致）。
- `docs/README.md` 唯一权威矩阵缺 `guides/modeling-environment.md` 行；`ENV_SETUP.md:3` 与 `workspace-layout.md:29` 称其为"环境唯一说明"，与 `docs/README.md:13`（环境归 `data-reproducibility.md`）及该指南自述 `:3` 矛盾 → 统一措辞为"安装与自检操作步骤"。
- 冗余收敛：`sandbox/` 语义在 `workspace-governance.md:119`、`workspace-layout.md:108-115`、`README.md:63-66,183` 三处；`README.md:106-151` 复制 `tools/README.md` 用法；两套 `rules.md` 的"使用规则"平行且已漂移（`cumcm:51-56` 4 条 vs `mcm-icm:45-50` 5 条，`cumcm:15` 与 `:21` 对"30 页是内部门禁还是官方规则"自相矛盾）。
- `plan/…多赛事通用化重构方案.md:420` 举例编号 `OFFICIAL-MCM-001` 与实现 `OFFICIAL-MCM-ICM-001` 不一致（历史方案文档，按现状注记即可，不改结论）。

### T12 回归与测试覆盖

- 现有测试仅覆盖 `contest: cumcm`（`tools/tests/*`），无 `mcm`/`icm` 解析、无"无 `body_page_minimum`"分支、无 `--contest icm` 端到端。T4–T10 落地后须补：Core/两 profile 契约加载断言、`mcm` 无页数下限分支、`--contest icm` 初始化 + `xelatex` 编译出 PDF 并逐页核页眉页数、`--contest cumcm` 数值与产物不变；并跑 `pytest tools/tests` 与 `tools/check-workspace-layout.py`。
