# 数学建模工作区环境与目录说明

本文档是本机环境、目录职责和常用命令的说明手册。工作区工程门禁以 `数学建模工作区_Agent强制规范.md` 为准，论文内容与版式以 `数学建模论文写作_Agent强制规范.md` 为准。目标是让每一次需求都具有唯一项目目录，让数据、代码、图片、论文、证据和交付物可追溯，避免根目录散落文件或出现多个“最终版”。

## 一、必须遵守的六条规则

1. **一个正式需求对应一个项目目录。** 新需求先放 `00-inbox/`，明确题目后在 `projects/` 下建立项目。
2. **原始数据只读。** 原题与附件分别归入 `01-problem/` 和 `02-data/raw/`；程序不得覆盖原文件。
3. **代码与产物分离。** 程序放 `03-models/`，运行生成的图片、表格、指标和日志放 `04-results/`。
4. **一个权威来源。** 论文只以 `06-paper/main.tex` 为权威源文件；提交 PDF 只以 `08-delivery/paper.pdf` 为权威交付物。
5. **缓存不得混入成果。** 临时渲染、调试截图、字体缓存和中间文件统一放 `tmp/`，任务结束清理。
6. **根目录保持干净。** 根目录只保留环境、全局规范、入口说明及系统文件；禁止直接新增项目脚本、结果图片或临时文档。

## 二、工作区根目录说明

| 路径 | 具体用途 | 应该放什么 | 不应该放什么 |
|---|---|---|---|
| `00-inbox/` | 新需求临时入口 | 用户刚提供且尚未归类的题目、附件、需求说明；每次需求建独立子目录 | 长期项目成果、运行结果 |
| `projects/` | 正式项目总库 | 每个项目一个 `<contest>-<year>-<problem>/` 目录 | 单独散落的图片或脚本 |
| `shared-tools/` | 跨项目通用工具 | 环境自检、通用转换、通用审计辅助程序 | 某一道题专用的模型代码 |
| `templates/` | 通用模板 | 论文框架、Word/LaTeX模板、清单模板 | 已填写的正式论文 |
| `paper-library/` | 优秀论文参考资料 | 优秀论文 PDF、阅读卡片、差距审计和索引 | 当前项目的正式引用产物或照抄内容 |
| `paper-system/` | 生产系统规范 | 命名规范、生产流程说明 | 某个项目的结果文件 |
| `archive/` | 历史归档 | 旧目录结构、已停用版本、迁移前快照 | 当前权威文件；代码不得从这里读取 |
| `tmp/` | 可删除临时区 | PDF渲染页、调试截图、Matplotlib缓存、解压文件 | 唯一副本、原始数据、最终成果 |
| `.venv-modeling/` | Python 3.12 建模环境 | 已安装依赖和解释器 | 项目代码、数据、Notebook |
| `.codex/` | Codex 本地 Skills | 论文生产和审校 Skill | 普通项目文件 |
| `.agents/`、`AGENTS.md` | Agent 工作规则 | 全局路由与强制门禁 | 运行结果 |
| `.gitignore` | 缓存排除规则 | Python、LaTeX、Notebook 与临时目录的忽略模式 | 项目业务配置 |
| `requirements-modeling.txt` | 环境依赖清单 | 已验证依赖版本 | 项目专属参数 |
| `数学建模工作区_Agent强制规范.md` | 全局工作区规范 | 目录、环境、版本、复现、证据、审校和交付门禁 | 论文正文写法 |
| `数学建模论文写作_Agent强制规范.md` | 全局论文规范 | 评审可见的结构、内容、学术表达、排版和论文附录要求 | 环境安装、运行入口和日志说明 |

当前的旧式根目录快照已经隔离到 `archive/legacy-root-layout/`。其中内容只用于追溯，不再作为运行、写作或交付的权威来源。

## 三、正式项目的标准目录

正式项目路径固定为：

```text
projects/<contest>-<year>-<problem>/
├── 00-admin/
├── 01-problem/
├── 02-data/
│   ├── raw/
│   └── processed/
├── 03-models/
├── 04-results/
│   ├── figures/
│   ├── tables/
│   ├── metrics/
│   └── logs/
├── 05-evidence/
├── 06-paper/
│   ├── figures/
│   └── tables/
├── 07-review/
└── 08-delivery/
```

### 1. `00-admin/`：项目管理

放项目标识、运行手册、环境记录、随机种子、阶段状态和交付清单。不得放模型输出或原始数据。

### 2. `01-problem/`：题目与需求

放原题、赛题说明、附件清单、问题拆解和必须输出量清单。推荐文件：`problem-statement.pdf|docx`、`problem-checklist.md`。这里不放清洗后的数据。

### 3. `02-data/raw/`：只读原始数据

只放用户或赛题提供的原始附件。文件进入后不得覆盖、改名后反复修改或由程序写回；必要时设置只读属性。当前项目使用 `attachment-01.xlsx` 至 `attachment-03.xlsx`。

### 4. `02-data/processed/`：可再生数据

放由 `03-models/` 程序生成的清洗数据、特征矩阵、训练/验证划分和中间数据。每个文件必须能够由代码重新生成，不允许手工修改关键数值。

### 5. `03-models/`：代码、算法与参数

放所有项目专用程序、配置和参数。命名采用 `q01-<purpose>.py`；跨问题入口可用 `q00-run-all.py`。Notebook 若只是探索可建 `03-models/notebooks/`，最终结论必须沉淀为可运行脚本。

禁止把项目脚本放到工作区根目录或 `04-results/`。

### 6. `04-results/figures/`：程序生成图片

放模型流程图、统计图、敏感性曲线、可视化结果。图片必须由代码生成并优先使用中文标题、坐标轴和图例。推荐命名 `q01-figure-001-topic.png`，重要图片同时保留 PDF 或 SVG 矢量版。

### 7. `04-results/tables/`：程序生成表格

放 CSV、XLSX 和用于论文的机器可读结果。摘要和正文中的具体数值必须追溯到这里或 `metrics/`，不得从控制台抄数。

### 8. `04-results/metrics/`：指标

放 JSON、YAML 等结构化评价指标，例如 AUC、误差、置信区间和最优目标值。一个具体数值只保留一个权威机器来源。

### 9. `04-results/logs/`：运行日志

放数据审计、模型训练、约束检查和复现运行日志。日志命名应包含用途和时间，不放无意义调试输出。

### 10. `05-evidence/`：证据与文献台账

放 `evidence-index.csv`、`literature-ledger.csv` 和内部过程记录。其中，`literature-ledger.csv` 是项目参考文献的检索与核验台账，用于登记 citation key、题名、作者、年份、DOI 或原始来源 URL、检索日期、正文用途及核验状态；文献只有在实际打开原始来源并完成登记后才能引用。每个论文主张必须指向数据、代码、日志、表格、图片、推导或已核验文献。

### 11. `06-paper/`：论文工作区

`main.tex` 是唯一权威论文源文件；参考文献排版库统一放在 `references.bib`，其条目必须与 `05-evidence/literature-ledger.csv` 中已经核验且被正文实际引用的记录一致；`figures/` 和 `tables/` 只保存论文实际引用的副本；`main.pdf` 是编译预览。LaTeX 的 `.aux`、`.log`、`.out` 等文件属于缓存，审校完成后清理。

### 12. `07-review/`：独立审校

放自动审校结果、人工审稿记录、匿名性检查、差距清单和修订记录。不得在该目录悄悄修改论文权威源文件。

### 13. `08-delivery/`：最终交付

只放提交所需文件和 `file-list.md`。正式论文固定为 `paper.pdf`；若当届竞赛要求其他命名，仅在交付时复制并改为官方文件名。禁止放缓存、源数据和未确认草稿。

## 四、新需求应该放在哪里

1. 在 `00-inbox/` 新建 `yyyy-mm-dd-short-name/`。
2. 把用户原始需求保存为 `request.md`，附件原样放入该目录。
3. 若只是一次性问答，不产生文件，可在完成后清空该目录。
4. 若形成正式数学建模项目，使用初始化脚本在 `projects/` 创建项目，再把原题和附件分别归入 `01-problem/`、`02-data/raw/`。
5. 完成归档后清空对应 inbox 子目录，避免存在两套原始附件。

新项目命令：

```powershell
.\.venv-modeling\Scripts\python.exe .codex/skills/cumcm-paper-production/scripts/init_cumcm_project.py --root projects --contest cumcm --year 2026 --problem a
```

## 五、当前项目位置与运行方式

当前项目：`projects/cumcm-2020-c/`。

```powershell
# 1. 环境自检
.\.venv-modeling\Scripts\python.exe shared-tools/check-modeling-env.py

# 2. 全量计算；约 2 分钟
.\.venv-modeling\Scripts\python.exe projects/cumcm-2020-c/03-models/q00-run-all.py

# 3. 扩展检验与中文图表
.\.venv-modeling\Scripts\python.exe projects/cumcm-2020-c/03-models/q00-expand-analysis.py

# 4. 生成论文附表
.\.venv-modeling\Scripts\python.exe projects/cumcm-2020-c/03-models/q00-make-paper-appendices.py

# 5. 编译论文（在论文目录执行两次）
Push-Location projects/cumcm-2020-c/06-paper
xelatex -interaction=nonstopmode -halt-on-error main.tex
xelatex -interaction=nonstopmode -halt-on-error main.tex
Pop-Location

# 6. 发布审校
.\.venv-modeling\Scripts\python.exe .codex/skills/cumcm-paper-audit/scripts/audit_cumcm_project.py projects/cumcm-2020-c --phase release
```

脚本会把 Matplotlib 缓存写入 `tmp/matplotlib/`。任务结束可以删除 `tmp/` 下的缓存，不影响结果复现。

## 六、Python 环境

- 环境目录：`.venv-modeling/`
- Python：64 位 Python 3.12
- 精确依赖：`requirements-modeling.txt`
- 常用包：NumPy、SciPy、pandas、Matplotlib、scikit-learn、statsmodels、PuLP、OR-Tools、pymoo、NetworkX、openpyxl、JupyterLab。

不激活环境时统一使用：

```powershell
.\.venv-modeling\Scripts\python.exe <script.py>
```

Graphviz 的 Python 接口 `pydot` 已安装，但系统 `dot.exe` 可能仍需另行安装。PyTorch 不默认安装，只有模型确实需要时才单独评估。

## 七、命名规则

- 项目目录：`<contest>-<year>-<problem>`，例如 `cumcm-2026-a`。
- 程序：`q01-purpose.py`；总入口：`q00-run-all.py`。
- 图片：`q01-figure-001-topic.png`。
- 表格：`q01-table-001-topic.csv`。
- 指标：`q01-metrics.json`。
- 日志：`q01-run-yyyymmddThhmmss.log`。
- 论文源：`06-paper/main.tex`。
- 提交论文：`08-delivery/paper.pdf`。

项目文件名优先使用小写 ASCII 字母、数字和连字符；中文只用于论文标题、图表标题和官方要求的交付文件名。禁止使用“最终版”“最新版”“修改版2”等不稳定命名。

## 八、任务结束检查

- 新需求已从 `00-inbox/` 归档到正式项目。
- 原始数据只存在于项目 `02-data/raw/`，且保持只读。
- 所有代码位于 `03-models/`，图片和表格位于 `04-results/`。
- 论文权威源为 `06-paper/main.tex`，交付物为 `08-delivery/paper.pdf`。
- 关键主张进入 `05-evidence/evidence-index.csv`。
- 发布审校通过，且 `tmp/` 与 LaTeX 缓存已清理。
- 工作区根目录没有散落脚本、图片、表格或项目 PDF。
