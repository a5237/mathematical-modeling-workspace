# 论文写作规范

> 适用范围：数学建模竞赛论文的内容组织、问题分析、模型论证、结果叙事、学术表达、引用、附录内容与匿名性；当届赛事特定要求由项目赛事 profile 的官方基线规定。
>
> 核心原则：**论文围绕题目形成“数学化—求解—回答—验证”的证据闭环，内容职责不得被排版或工程记录替代。**
>
> 边界说明：数据与复现、模型执行、纯排版和图片分别由 `docs/standards/data-reproducibility.md`、`docs/standards/modeling-execution.md`、`docs/standards/paper-formatting.md` 和 `docs/standards/paper-figures.md` 管理。工程记录原则上不写入正文；凡直接影响模型定义、求解过程或结论可信度的科学设置，仍必须在论文中说明。

```toml machine-contract
body_word_minimum = 5000
```

上方字段是 `PW-LEN-001` 的稳定机器参数；自然语言写法和章节位置不参与程序解析。

---

## 0. 规范用语与执行级别

规范词的统一含义执行 `docs/standards/workspace-governance.md` 第 1 节。本文件中的内容自适应不得删除必要的模型、求解、结果、验证、图表、文献或排版职责，也不得把质量建议升级为所有题目的固定方法。

---

## 1. 规则优先级与现行官方基线

跨阶段冲突顺序只执行 `docs/standards/workspace-governance.md` 第 1 节；赛题输出要求属于当前项目明确约定，模板只是本规范与排版规范的实现。年度规则、核对日期、生效边界和官方链接只以当前项目赛事 profile 的官方基线（`config/contests/<profile>/rules.md`）为准。

### 1.1 官方规则与工作区增补

当前项目赛事 profile 的官方基线规定当届官方底线；论文内容由本规范增补，字体、字号、行距和版式由 `docs/standards/paper-formatting.md` 增补，内部发布门禁由 `docs/standards/paper-quality-audit.md` 管理。工作区增补不得冒充全国组委会统一规定，官方或赛区要求更严格时从其规定。

### 1.2 写作启动门禁

正文和摘要写作前必须通过 `docs/guides/pre-writing-learning.md` 的 `PWL-GATE-001`，并确认 `WG-MODEL-001` 与 `WG-EVID-001` 已满足当前写作所需状态。学习数量、记录字段、模型选择和证据状态只以对应权威文件为准。

### 1.3 唯一写作框架与无状态交付

1. 每次启动正式论文写作前，必须先将当前项目赛事 profile 的论文框架（`resources/templates/contests/<profile>/paper-framework.tex`）复制为项目 `06-paper/main.tex`，并以该文件作为唯一权威论文源；初始化脚本已经完成复制时，不得再次覆盖。
2. 后续写作、修订、排版和编译只维护 `06-paper/main.tex` 及其实际引用的图表、文献资源，不得另建平行论文源、自由稿或替代章节框架；纯排版执行 `docs/standards/paper-formatting.md`。
3. 每次修改后的论文源和生成文件必须保持 **Stateless Deliverable（无状态交付）**：内容本身可直接进入审校与交付，不设置“修改说明”“版本变化”“补丁摘要”“修改理由”“写作过程”等章节或段落，也不夹带面向修改过程的解释。必要的工程与审校记录分别保存在 `00-admin/` 和 `07-review/`，不得进入论文。

---

## 2. 通用高质量写作原则

### 2.1 全文主线与证据闭环

论文必须形成清晰主线：

```text
题目要求
→ 问题抽象与任务拆解
→ 数据理解或机理分析
→ 变量、指标与模型建立
→ 算法或方法求解
→ 主要结果
→ 结果解释与验证
→ 最终方案、评价与推广
```

每个子问题都必须形成“提出—数学化—求解—回答—检验”的闭环。不得出现只有模型没有结果、只有结果没有推导、只有图表没有解释、只有程序没有数学表达的断裂章节。

### 2.2 模型适度与内容自适应

1. 模型复杂度由题目决定，不以模型数量或算法名称评价质量。
2. 简单模型足以解决问题时，应优先采用结构清楚、可解释、可验证的方法。
3. 使用复杂模型时，必须说明简单方法为何不足、复杂模型新增了什么能力、是否获得可核验增益。
4. 不同题型采用不同数学表达；非优化问题不得强行设置目标函数和 `s.t.`。
5. 题型不适合某项图表或验证时，应使用推导、证明、算例、边界检查或其它等效证据，不得直接删去论证职责。

### 2.3 物理、工程与连续系统问题

处理物理机理、工程设计、微分方程、连续介质、传热传质或数值模拟问题时，应：

1. 从守恒定律、物理定律、几何关系或可靠机理出发，不直接堆叠经验公式；
2. 说明真实场景如何被降维、分层、理想化及忽略哪些次要因素；
3. 完整列出控制方程、初始条件、边界条件、界面条件、耦合条件和定义域；
4. 不能解析求解时，比较候选数值方法的稳定性、精度和适用条件；
5. 写清必要的离散过程、网格或步长、矩阵关系和求解方法，不能只写“调用软件求解”；
6. 使用实测值、基准解、残差、收敛性、守恒量、网格无关性或误差指标进行量化验证；
7. 对关键参数、边界条件和最优方案进行灵敏度、扰动或最优性检查；
8. 在模型评价中回扣简化假设、数值误差、计算可行性和适用边界。

图片证据的表达强度执行 `PW-FIG-001`，并必须与 `PW-VAL-001` 要求的数值指标或明确判据共同支撑结论。

### 2.4 数据驱动、评价、预测与决策问题

处理统计、评价、分类、预测、机器学习或数据驱动决策问题时，应：

1. 先完成数据审计，再进行特征构造、训练或优化；
2. 按业务含义、数学作用或因果位置解释特征，不把大量原始字段直接堆入模型；
3. 指标必须说明方向性、计算方式、实际含义及其与题目目标的关系；
4. 原则上设置可解释的基准模型，再说明复杂模型相对基准新增的能力和实际增益；
5. 使用与任务匹配的数据划分和评价指标，不得只报告训练集表现；
6. 类别不平衡、时间依赖、空间相关或对象重复时，采用相应验证和防泄漏方案；
7. 集成模型说明基模型、融合方式、权重或投票依据，以及相对单模型的增益；
8. 预测概率、评分或聚类标签必须继续转化为题目要求的排序、分配、额度、类别、路径、策略或方案；
9. 结果表必须直接回答题目，不能让读者自行从中间概率或原始输出推导最终答案；
10. 正文保留核心模型、关键比较和主要结果，超长明细放入附录。

高准确率、高拟合度或高评价指标不能单独证明模型优秀，必须同时说明数据划分、评价口径、泄漏检查、稳健性和适用范围。

### 2.5 多子问题、情景变化与模型继承

当各子问题具有递进关系时，应：

1. 在问题分析中说明各问依赖关系和整体技术路线；
2. 明确后续问题继承了哪些数据、变量、模型、参数和结论；
3. 单独列出新增或修改的目标、约束、参数、情景和输出；
4. 不重复粘贴完全不变的公式，也不得只写“同前”而省略关键修改；
5. 情景变化形成“影响因素—参数或指标变化—模型调整—结果变化”的逻辑链；
6. 每一问都给出独立、可识别的最终答案和验证材料。

### 2.6 真实性、可核验性与全文一致

- 不得编造原始数据、参数来源、计算结果、模型性能、软件输出、文献或检验结论。
- 所有具体数值必须来自题目数据、可核验资料、明确推导或真实运行的程序输出。
- 缺少必要数据时，必须说明缺口，并采用有依据的假设、符号化表达、区间结果或条件性结论。
- 每个英文缩写首次出现时使用“中文名称（英文全称，英文缩写）”；摘要与正文分别视为独立文本，首次出现均需解释。
- 论文图片的职责边界、图型选择、生成与视觉审校统一执行 `PW-FIG-001`。

---

## 3. 固定论文结构与章节适配

论文默认使用以下一级结构。题目有几问，就设置几个对应分问；少于四问删除多余分问，多于四问按同样结构顺延。

```markdown
# 题目

## 摘要
关键词：

——分页执行当前项目赛事官方基线与 `PW-FMT-001`——

## 一、问题重述
### 1.1 问题背景
### 1.2 问题要求

## 二、问题分析
### 2.1 问题一的分析
### 2.2 问题二的分析
### 2.3 问题三的分析
### 2.4 问题四的分析

## 三、模型假设

## 四、符号说明

## 五、模型的建立与求解
### 5.1 问题一模型的建立与求解
#### 5.1.1 问题一的建模准备
#### 5.1.2 问题一模型的建立
#### 5.1.3 问题一模型的求解
#### 5.1.4 问题一结果的分析

### 5.2 问题二模型的建立与求解
#### 5.2.1 问题二的建模准备
#### 5.2.2 问题二模型的建立
#### 5.2.3 问题二模型的求解
#### 5.2.4 问题二结果的分析

### 5.3 问题三模型的建立与求解
#### 5.3.1 问题三的建模准备
#### 5.3.2 问题三模型的建立
#### 5.3.3 问题三模型的求解
#### 5.3.4 问题三结果的分析

### 5.4 问题四模型的建立与求解
#### 5.4.1 问题四的建模准备
#### 5.4.2 问题四模型的建立
#### 5.4.3 问题四模型的求解
#### 5.4.4 问题四结果的分析

## 六、模型的评价与推广
### 6.1 模型的优点
### 6.2 模型的缺点
### 6.3 模型的改进与推广

## 七、参考文献

——附录分页执行当前项目赛事官方基线与 `PW-FMT-001`——

## 附录
### 附录1 问题一所用代码
### 附录2 问题二所用代码
### 附录3 问题三所用代码
### 附录4 问题四所用代码
```

### 3.1 正文字数门禁（`PW-LEN-001`）

正文叙述性内容不得少于 **5000 字**。统计范围为“问题重述”起至“模型的评价与推广”结束的可读正文文字；摘要、关键词、参考文献、附录、代码、纯公式符号以及图表内部文字不计入。中文汉字、独立英文单词和数字串均按 1 字计。不得用无关背景、重复论述或空泛文字凑足篇幅；当届官方或赛区要求更严格时，从其规定。

### 3.2 结构适配边界

- 一级章节的名称、顺序和职责固定，不得自行新增、删除、改名、合并或调序；只有比赛官方要求或用户明确要求与本结构冲突时才可调整，并必须保留全部章节职责。
- 子问题数量按题目实际增减；同一问使用多个模型时，只可在对应分问的模型建立或求解下增设确有内容依据的更低级标题，不得据此新增一级章节或与固定章节并列的独立部分。
- 不得把第二章“问题分析”与第五章“模型建立”合并。
- 第五章每问的“建模准备”必须保留，负责就近给出模型建立前必需的坐标、几何判定、数据变换、指标定义、机理简化、继承关系或预备公式；不得再使用“模型分析”作为该小节名称，也不得直接复制第二章。
- 每个可独立命名的主模型、局部模型或模型组件都必须设置“模型名称——作用”的功能性小标题；标题样式执行 `PW-FMT-001`，必要图示执行 `PW-FIG-001`。
- 数据预处理、模型检验、敏感性分析或方案说明内容较多时，只可在相应固定章节或分问内设置更低级小节，不得另起一级部分或打乱论文主线。
- 摘要页、目录和官方分页执行当前项目赛事官方基线。

---

## 4. 标题、摘要与关键词

### 4.1 论文标题

1. 标题应体现研究对象，并优先体现正文真正起主导作用的核心模型或算法。
2. 宜控制在一行内，不得为追求短而丢失研究对象。
3. 可采用“基于 XXX 模型的 XXX 问题研究”“基于 XXX 模型与 XXX 方法的 XXX 研究”等形式。
4. 当研究对象足够明确时，可采用简洁问题型标题，不必机械使用“基于”。
5. 不得堆砌正文未重点使用的算法名称，不得使用“某问题研究”“数学模型分析”等空泛标题。

### 4.2 摘要页硬要求

1. 摘要专用页的组成和分页执行当前项目赛事官方基线。
2. 摘要控制在一个版面内，使评审仅阅读第一页即可知道研究内容、逐问方法、关键结果和可信度。
3. 不得在摘要中引用图、表、公式或参考文献编号。
4. 不得写程序目录、软件与库版本、运行入口、文件路径、长公式推导或大段背景常识。
5. 摘要的页面、字体、首行缩进和分页执行 `docs/standards/paper-formatting.md`。

### 4.3 摘要固定逻辑

#### 第一段：总体概述

通常使用 2—3 句话、约 3—5 行，包含：

- 问题背景或研究意义；
- 总体建模主线；
- 主要模型或方法类别；
- 解决的核心问题或总体成果。

第一段不得展开详细求解过程，也不得无序罗列模型名称。

#### 各子问题摘要

每个子问题单独成段，依次说明：

1. 本问任务或数学类型；
2. 关键建模思路；
3. 核心模型或方法；
4. 必要的数据与求解方式；
5. 题目要求的关键结果；
6. 结果意义、检验或可信度。

计算型问题必须给出题目要求的核心数值与单位；开放型问题必须给出明确方案、排序、类别、策略或结论。不得只写“得到了较好结果”。

#### 最后一段：总结与升华

有实质内容时，可概括模型体系、主要优点、稳健性、局限、适用范围或推广价值；没有实质信息时可省略，不写空泛套话。

摘要中核心模型、关键数值、最终方案和结论性短语的视觉强调执行 `PW-FMT-001`。

### 4.4 关键词

1. 一般为 4—6 个；
2. 优先覆盖研究对象、研究目的、核心模型、求解算法和验证方法；
3. 使用统一分隔符，推荐中文分号；
4. 不使用“数学建模”“问题分析”“MATLAB”等过宽词作为主要关键词；
5. 缩写作为关键词时，摘要中应已解释全称。

---

## 5. 正文章节写作要求

### 5.1 一、问题重述

#### 5.1.1 问题背景

- 用自己的语言概括真实背景、研究对象和应用意义；
- 删除故事化、宣传性和无关描述；
- 不提前展开模型、算法和结果；
- 原则上控制在 1—3 个自然段。

#### 5.1.2 问题要求

- 将题目各问转化为明确任务；
- 写清输入、条件、约束和输出；
- 对题目要求生成的附件、表格、预测文件等单独注明；
- 不得照抄原题，不得改变题意。

### 5.2 二、问题分析

第二章必须按子问题分别说明：

- 子问题的数学本质；
- 已知量、未知量和主要约束；
- 数据特征和核心困难；
- 与前后问题的关系；
- 拟采用的模型或方法；
- 选择该方法的理由；
- 预期输出和验证路径。

复杂题宜在章末设置总体技术路线或整体建模思路；需要图示时执行 `PW-FIG-001`，并确保其与后文章节和实际计算逻辑一致。

### 5.3 第五章各问的“建模准备”

“建模准备”位于每问“模型建立”之前，用于提供紧邻公式推导且不可缺少的预备条件。其职责按题型选取：

- 建立坐标系、时间基准、对象索引、几何关系或空间判定方法；
- 给出数据变换、归一化、特征构造、指标口径或中间量定义；
- 说明机理简化、状态划分、边界识别或关键预备公式；
- 交代从前问继承的变量、参数和结果，以及本问新增或改变的部分；
- 明确随后各局部模型的输入、输出与衔接顺序。

简单问题可用 3—8 行完成；复杂几何、物理机理或数据流程可配公式，并按 `PW-FIG-001` 使用必要图示适度展开，不设机械字数上限。第二章“问题分析”回答“本问难在哪里、为何选择该方法”，第五章“建模准备”回答“正式建模前具体准备什么”；两者不得互相复制，也不得以“前文已经分析”为由删除本节。

### 5.4 章节衔接与段落闭环

1. 各节开头说明本节目的及与上一节的关系；
2. 各节结尾给出本节得到的模型、参数、结论或下一步输入；
3. 重要图、表和公式采用“文字引入—对象呈现—结果解释”的闭环；
4. 不得在章节之间突然切换模型、数据口径或符号而无过渡；
5. “综上”“由此”“因此”等连接语必须对应真实推导关系。

### 5.5 三、模型假设

1. 原则上用纯文字编号列举，不放无关图表和复杂公式。
2. 数量按需要确定，常见为 4—7 条，但不得凑数。
3. 假设必须与模型直接相关，并说明物理、业务或统计合理性。
4. 不重复题目已明确给出的事实，不以假设直接消除核心难点。
5. 不写“题目数据真实可靠”“不考虑一切偶然因素”等空泛假设。
6. 对结果影响显著的假设，必须在误差、灵敏度或模型评价中回扣。
7. 数据驱动模型应区分业务假设、统计假设和数据处理规则。

### 5.6 四、符号说明

1. 集中列示高频核心符号，局部临时变量可在公式附近定义；
2. 同一符号全文含义唯一；
3. 不得把模型名、软件名和无需解释的常用数学符号大量塞入符号表；
4. 符号表的列、单位栏、字体和三线表格式执行 `docs/standards/paper-formatting.md`。

---

## 6. 数据理解与预处理

### 6.1 数据审计内容

完整数据审计按 `docs/standards/data-reproducibility.md` 保存在工作区。论文只呈现会影响模型定义、训练与验证、结果解释或结论可信度的审计发现和处理规则，并说明其对样本范围与结论的影响。

### 6.2 预处理规则

数据处理方法、划分、防泄漏和代码一致性只由 `docs/standards/data-reproducibility.md` 与适用的 `PW-VAL-001` 定义。论文不得只写“进行了数据预处理”，而应从权威数据审计记录中说明影响模型或结论的处理对象、规则、理由及样本范围变化；未影响评审理解的工程细节留在工作区。

### 6.3 可视化预处理

用于缺失、异常、离散程度、偏态、时间变化、类别不平衡、变量关系或空间分布的数据图，统一执行 `PW-FIG-001`；不得绕过其中的论证目标、数据剖析和证据要求。

---

## 7. 模型建立的强制写法

### 7.1 总原则：先解释，后数学化

每个子问题必须遵循：

```text
建模准备与关键问题转化
→ 数据处理或机理简化
→ 变量、参数和评价指标定义
→ 按“模型名称——作用”组织局部关系与组成公式
→ 完整模型集中汇总
→ 模型输出与题目要求对应
```

不得一进入模型建立就堆放目标函数、约束或大段公式。

### 7.2 问题转化与模型选择

必须说明：

1. 原问题如何抽象为数学问题；
2. 核心对象、主要矛盾和待求量；
3. 保留和忽略了哪些现实因素；
4. 本问属于优化、预测、评价、分类、微分方程、仿真或其它何种任务；
5. 为什么选择当前模型，候选方法为何不作为主模型；
6. 模型输入、输出与题目目标如何对应；
7. 与前面子问题的继承、修改或扩展关系。

建模与编码阶段的算法资源查阅、候选比较、主模型数量和库外方法偏离记录执行 `docs/standards/modeling-execution.md` 的 `WG-MODEL-001`。本节只要求论文解释与评审理解有关的模型选择理由，不重复工程记录字段。

### 7.3 变量、参数与指标

按实际需要定义决策变量、状态变量、输入输出变量、模型参数、中间变量、风险收益指标、随机变量、时间/空间/对象索引、单位、定义域和取值范围。

任何符号必须在首次使用前或首次使用处解释；高频符号与符号说明表一致。

### 7.4 局部关系与公式推导

应按模型逻辑分别建立收益、成本、风险、概率、损失、似然、评价指标、守恒关系、状态转移、控制方程、初边值条件及资源或业务约束。

每个局部公式出现前必须说明建立依据，出现后解释各项数学含义和实际意义。不得连续堆放多个公式而无文字说明。

### 7.5 局部模型的标题与作用

每个可独立命名的主模型、局部模型、机制模块或算法阶段都必须设置功能性小标题；同一问包含多个时逐个设置，只有一个时也不得只用“问题一模型的建立”代替具体模型名。标题采用以下任一语法并全文统一：

```text
余弦效率模型——量化入射夹角造成的能量损失
路径规划模型（作用：生成满足容量与时窗约束的访问顺序）
```

具体要求：

1. 小标题必须同时给出可识别的模型名称和本模型在当前子问题中的作用，不得只写“模型一”“模型二”“计算模型”或算法缩写。
2. 小标题置于对应解释、图示和公式块之前，不得把模型名称埋在正文中、公式后或图题中。
3. 每个局部模型按“名称与作用—输入输出—必要图示—关系或公式—本模块输出及去向”组织；简单模块可合并说明，但不得省略名称与作用。
4. 局部模型是否需要图示以及图示的职责、类型、内容和质量统一执行 `PW-FIG-001`。
5. 同一问的局部模型小标题使用同一语法，模型名称在摘要、正文、图表和完整模型汇总中保持一致；字号、字重、左边距和同页控制执行 `PW-FMT-001`。

### 7.6 每问的完整模型汇总

完成局部推导后，每个子问题必须集中给出一次可独立阅读的完整模型，包括实际需要的：

- 目标函数或核心方程；
- `s.t.` 约束；
- 初始条件、边界条件和耦合条件；
- 变量范围、定义域或可行域；
- 参数条件；
- 输入与输出。

完整模型之后逐项说明目标、主要条件和输出如何回答题目。存在多个局部模型或关系组时，应集中呈现并用与功能性小标题一致的短语标出各组作用，使读者能由“模型名称—作用”定位到对应关系；左大括号、对齐和编号执行 `docs/standards/paper-formatting.md`。除等价变形或求解需要外，汇总后不得新增未纳入汇总的核心关系。

### 7.7 优化模型

必须集中列出决策变量、目标函数、`s.t.` 约束、变量范围、多目标处理方法以及目标与约束的实际意义。

优化模型先说明目标，再集中说明约束及其实际含义。`s.t.`、多行对齐、左大括号和 LaTeX 结构执行 `docs/standards/paper-formatting.md`。

多目标模型必须说明加权和、层次优化、ε-约束、Pareto 前沿或其它方法的理由；不同量纲目标先无量纲化。权重不得无依据指定，关键权重应做稳定性或敏感性检查。

整数、离散或组合决策不得先求连续解后直接四舍五入作为最终答案。取整、修复或启发式后处理后，必须重新检查全部约束、目标值变化和方案可行性。

### 7.8 微分方程与物理模型

必须集中列出控制方程、初始条件、边界条件、界面或耦合条件、定义域、参数物理意义与单位及简化假设。从三维降至二维/一维、连续系统离散化或多层系统耦合时必须说明依据。

### 7.9 统计与机器学习模型

必须集中说明预测目标或标签、特征含义、数据划分、核心预测函数、损失/似然/学习准则、参数估计、超参数选择、评价指标、基准模型和防泄漏措施。

分类模型不得只给准确率；回归模型不得只给拟合曲线；聚类模型不得只给聚类图而无聚类数选择和解释。

### 7.10 评价模型

必须集中说明指标体系、指标方向、标准化、权重计算、综合评价函数、排序或分类规则、权重稳定性和指标冗余检查。每个指标必须解释“为什么影响评价对象”和“数值增大意味着什么”。

### 7.11 算法与仿真模型

必须集中说明输入输出、状态变量、核心规则或迭代公式、关键参数、初始状态、停止条件、重复仿真次数和结果汇总方式。性能与复杂度是否需要记录及其证据要求执行 `docs/standards/modeling-execution.md`；正文只保留影响方法选择或结论理解的内容。

---

## 8. 模型求解与算法说明

每个子问题的模型求解必须说明：

1. 采用的求解方法、算法或求解器及必要选择依据；
2. 求解基本思路和关键步骤；
3. 对结果有实际影响的搜索范围、步长、网格、容差、样本量、超参数或停止条件；
4. 主要软件、求解器或计算工具名称；
5. 得到的关键数值、方案、预测、分类、评价或仿真结果；
6. 结果是否满足约束、量纲、边界条件和实际常识；
7. 论文中的步骤、公式、参数、图表与实际计算必须一致。

不得为满足形式而虚构迭代过程、搜索范围、随机种子或算法比较。解析求解、直接代入或简单代数计算可简要说明，无需机械拆分步骤。

### 8.1 方法选择与必要比较

当存在两种及以上真正可行的方法，且选择会显著影响精度、稳定性、计算效率、可解释性或可靠性时，应比较与本题有关的指标并说明选择依据。

稳定性、收敛性、网格无关性及不同初值一致性只在相关方法适用且会影响结论时检查。

### 8.2 求解步骤

复杂算法或多阶段计算宜使用编号步骤，内容与实际过程一致，例如：

1. 准备输入数据或初始参数；
2. 完成必要预处理或变量计算；
3. 建立待求方程、目标函数或迭代关系；
4. 执行核心计算或调用求解器；
5. 检查收敛、约束和边界条件；
6. 输出并解释关键结果。

步骤数量按实际过程确定。采用正式算法块时，依次写“算法 x：名称——作用”“输入”“输出”和 `Step 1, Step 2, ...`；算法标题加粗左对齐，输入输出必须与模型定义及程序实际接口一致。算法流程图统一执行 `PW-FIG-001`。

---

## 9. 结果呈现与分析

模型计算和每问验证义务只以 `docs/standards/modeling-execution.md` 的 `PW-VAL-001` 为准。本节规定这些结果和验证如何进入论文，不建立第二套验证项目或数量要求。

### 9.1 先给答案，再展开证据

每问的结果分析应先用一段话明确给出最终答案、实际含义与主要适用条件，再用表格、图形、指标、推导或案例解释依据。不得让评审阅读数页后仍无法确定结论。

结果表原则上包含对象编号或类别、核心输出、单位、必要误差或状态、最终决策或结论。正文展示代表性结果和关键汇总，超长明细放附录，并在正文指出位置。

表格的题注、字体、对齐、跨页和列宽执行 `docs/standards/paper-formatting.md`。
表后必须解释主要发现而不是逐行朗读。

### 9.2 结果可信度与结论强度

正文必须呈现足以让评审理解 `PW-VAL-001` 是否成立的关键判据、证据和判断，不得只报告“已完成验证”。工程日志和完整中间输出留在工作区。

“准确、稳定、最优、显著、优于”等词必须有相应证据。禁止仅写“由图可知模型准确”“结果符合实际”“效果很好”。

### 9.3 典型案例与解释

适合时选取最优、最差、中间、边界、异常或不同类别代表样本，说明模型为何产生对应结果，以增强可解释性和实际可操作性。

### 9.4 结果可视化的清晰性与直观性

结果分析中的数据图、诊断图、验证图和多面板组合图全部执行 `PW-FIG-001`。该控制项未通过时，相关子问题不得视为完成结果呈现或验证。

---

## 10. 模型评价、改进与推广

### 10.1 模型优点

必须结合实际证据说明，可从机理解释性、数据适应性、稳定性、精度、求解效率、特殊结构利用、决策可操作性、稳健性或规模扩展性中选择。每个优点应指出对应模型结构、实验结果或检验证据。

### 10.2 模型缺点

如实说明假设限制、数据限制、参数依赖、未考虑因素、局部最优风险、泛化能力、数值误差和实际应用条件。不得把缺点写成推翻全部论文的致命错误，也不得回避真实局限。

### 10.3 模型改进与推广

必须说明：

- 可推广到哪些对象、地区、行业或尺度；
- 推广时哪些变量、约束和参数需调整；
- 需要补充什么数据；
- 如何扩展到动态、多目标、随机或鲁棒场景；
- 推广后的验证方式。

不得只写“模型具有良好的推广性”。

---

## 11. 参考文献与引用

### 11.1 数量与组成（`PW-CITE-001`）

参考文献原则上不少于 **6 篇**。以下两本基础教材必须列入参考文献，并在正文介绍相应建模方法、算法或通用理论时实际引用：

```text
[1] 姜启源, 谢金星, 叶俊. 数学模型（第五版）[M]. 北京: 高等教育出版社, 2018.
[2] 司守奎. 数学建模算法与应用[M]. 北京: 国防工业出版社, 2011.
```

其余至少 **4 篇**应与题目背景、核心模型、算法、参数来源或验证方法直接相关。

这是本工作区的论文质量门禁，不冒充全国组委会统一规定。不得为了满足数量加入无关文献；若未能检索、核验并实际使用足够文献，应阻止发布而不是虚构或凑数。

### 11.2 文献质量

文献真实性、打开核验、来源定位和台账状态只执行 `WG-EVID-001`。论文侧还必须保证正文引用与文后编号一一对应，不加入正文未引用的文献，格式全文统一；专业背景、参数和算法宜优先使用原始论文、权威教材、标准、官方文档或高质量综述，在线资料须呈现足够来源信息和访问日期。

### 11.3 正文引用

- 引用放在对应事实、方法、公式或参数附近；
- 不在段末堆放多个无法判断对应关系的文献编号；
- 自行推导的公式不伪装成文献结论；
- 两本指定教材必须在正文实际方法介绍处引用，不能只出现在文末；
- 所有公开来源、数据集、他人成果和按官方要求披露的 AI 工具均应规范标注。

文献检索过程、DOI/URL 核验和文献台账记录在工作区；论文呈现规范的正文引用和参考文献表。

---

## 12. AI 工具使用、附录与匿名性

### 12.1 AI 工具使用

严格执行当前项目赛事 profile 的官方基线，按实际提交日期采用已生效的官方声明位置、措辞和详情文件名。完整提示方式、使用过程、采纳、人工修改和核验记录执行 `WG-AI-001`，不在正文堆砌工程台账。

### 12.2 附录结构与内容

1. 按题目分问或实际程序模块组织，子问题数量按题目增减。
2. 当届文件清单、源程序和无程序/无支撑材料措辞执行当前项目赛事官方基线，本节不复制年度提交要求。
3. 可放置正文不宜展开的长表、补充推导、算法伪代码、参数表和补充结果。
4. 附录代码与正文图、表和结果必须对应，不得包含无关测试或调试内容；代码附录说明其子问题及对应的正文对象。

附录和附件的分页、字体、公式及表格版式执行 `docs/standards/paper-formatting.md`。

环境安装、依赖锁定、运行命令、目录说明和完整复现手册放在工作区与支撑材料说明中，不写成正文式章节。

### 12.3 匿名性

拟交付的摘要、正文、附录、代码及其注释、文件名、图片属性与元数据、文档元数据、支撑材料和交付压缩包均不得出现队员、学校、指导教师、赛区、用户名或可反推出身份的本机路径。

---

## 13. 全文一致性检查

必须检查：

- 摘要与正文模型名称一致；
- 摘要关键数值与正文一致；
- 正文与真实计算输出一致；
- 图表与正文数值一致；
- 公式符号与符号表一致；
- 单位和小数位一致；
- 题目对象编号一致；
- 参考文献编号一致；
- 附录文件清单与实际支撑材料一致；
- 后续问题继承的参数、变量和结论与前问一致；
- 模型评价中的优缺点与实际验证结果一致。

所有摘要关键数值和最终决策都必须能在工作区证据索引中定位，但证据索引本身不作为正文章节。

---

## 14. 严格禁止事项

本规范前述“必须 / 不得”继续作为论文质量要求，但 Python 不通过解析自然语言逐条证明其满足。Final Audit 依据实际论文和证据独立判断，并重点阻断四类系统性违规：伪造或不可追溯内容、用图形或软件界面替代数学论证、通过堆砌模型或图表满足形式、以及用排版技巧掩盖缺失内容或破坏可读性。一般表达质量与建模洞察进入 Reviewer 评分和修改建议，不能由竞争力分数直接触发机器阻断。

---

## 15. 论文写作工作流

完整阶段与命令只以 `.codex/skills/modeling-paper-production/SKILL.md` 和 `docs/guides/paper-production.md` 为准。写作必须在 `WG-MODEL-001`、`WG-EVID-001` 与 `PWL-GATE-001` 通过后基于核验证据进行，并在发布前通过本规范、`PW-FMT-001`、`PW-FIG-001` 和 `PQA-RELEASE-001`。

---

## 16. 最终交付前强制检查表

本表只聚合控制项，不复述正文规则或阈值：

- [ ] 唯一论文源、固定结构、摘要与无状态交付符合第 1、3、4 节及当前项目赛事官方基线。
- [ ] `PW-LEN-001`、`WG-MODEL-001`、`WG-EVID-001` 与 `PWL-GATE-001` 已通过。
- [ ] 数据叙事、模型建立和求解符合第 6—8 节，并与数据复现和建模执行记录一致；每问均有完整模型和直接答案。
- [ ] `PW-VAL-001` 已通过，验证结论均有判据和可核验证据。
- [ ] 表格、公式、符号、单位、有效数字和版式符合 `docs/standards/paper-formatting.md` 的 `PW-FMT-001`，图片符合 `PW-FIG-001`。
- [ ] 模型评价、`PW-CITE-001`、附录、匿名性和全文一致性符合第 10—13 节及当前项目赛事官方基线。
- [ ] `WG-AI-001`、`WG-RELEASE-001` 与 `PQA-RELEASE-001` 已通过，正式提交前已重核官方来源。
