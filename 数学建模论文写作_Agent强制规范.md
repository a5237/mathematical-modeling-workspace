# 数学建模论文写作 Agent 强制规范

> 适用范围：全国大学生数学建模竞赛（CUMCM）及同类本科生数学建模竞赛论文的分析表达、模型论证、结果呈现、学术引用、排版和论文附录。
>
> 核心原则：**论文质量不得因规范重构而降级；工程职责可以迁移，学术论证、写作细则和排版门禁必须完整保留。**
>
> 边界说明：项目目录、环境安装、软件与库版本、运行入口、命令行、输入输出路径、日志、证据台账、AI 交互台账和完整复现手册由《数学建模工作区_Agent强制规范.md》管理，原则上不写入论文正文。凡直接影响模型定义、求解过程或结论可信度的科学设置，仍必须在论文中说明。

---

## 0. 规范用语与执行级别

- **必须 / 不得**：硬性要求，未满足不得发布。
- **应 / 原则上**：默认执行；确因题型不适用时，必须采用等效论证并说明理由。
- **宜 / 推荐**：质量提升项，应结合题目选择。
- **可**：允许但不强制。

不得以“内容自适应”为由删除必要的模型、求解、结果、验证、图表、文献或排版要求；也不得把“推荐”机械升级为所有题目必须采用的固定模型或固定图表。

---

## 1. 规则优先级与现行官方基线

发生冲突时按以下顺序执行：

1. 比赛当届官方规则、论文格式规范、AI 工具使用规定和赛区补充要求；
2. 用户在当前任务中的明确要求；
3. 赛题对输出内容和文件的要求；
4. 本规范；
5. `templates/摘要+论文框架.docx` 中可迁移的培训建议；
6. Agent 默认习惯。

本规范于 **2026-07-28** 核对的全国组委会基线包括：

- [全国大学生数学建模竞赛论文格式规范（2026 年修订稿）](https://www.mcm.edu.cn/html_cn/node/4cd596519c9eb9fbd866398f6df0caa3.html)；
- [全国大学生数学建模竞赛参赛规则（2026 年修订稿）](https://www.mcm.edu.cn/html_cn/node/9d8e511fe7a1447b35f53a82c908e2e0.html)；
- [全国大学生数学建模竞赛人工智能工具使用规定（2026 年试行）](https://www.mcm.edu.cn/html_cn/node/fef94648f2836ab6cc81586f4c38512b.html)。

正式参赛前必须重新打开全国组委会官网核对现行文件，不得仅凭本规范作最终合规判断。

### 1.1 2026 官方硬要求摘要

- 纸质论文使用 A4 纸，上下左右页边距至少 2.5 cm，左侧装订。
- 摘要专用页包括标题、摘要和关键词，原则上不超过一页；从摘要页开始以阿拉伯数字连续编页码。
- 正文从摘要下一页开始，不设目录，正文不超过 30 页；正文后为页数不限的附录。
- 附录包括支撑材料文件列表和建模所用全部完整、可运行的源程序；未使用程序或无支撑材料时按官方措辞说明。
- 摘要、正文、附录、文件名、代码、元数据和支撑材料不得泄露参赛者、学校或赛区身份。
- 电子论文为单独的 PDF 或 Word 文件，推荐 PDF；不含承诺书和编号专用页，第一页必须为摘要页。
- 论文与支撑材料的格式、大小、命名和提交方式按当届官方通知执行。

官方未统一规定的字体、字号、行距和颜色，本工作区仍执行本规范的统一排版要求，除非官方或赛区另有规定。

---

## 2. 通用高质量写作原则

### 2.1 全文主线与证据闭环

论文必须形成清晰主线：

```text
题目要求
→ 问题抽象与任务拆解
→ 数据理解或机理分析
→ 变量、指标与模型建立
→ 算法或方法求解
→ 主要结果
→ 结果解释与验证
→ 最终方案、评价与推广
```

每个子问题都必须形成“提出—数学化—求解—回答—检验”的闭环。不得出现只有模型没有结果、只有结果没有推导、只有图表没有解释、只有程序没有数学表达的断裂章节。

### 2.2 模型适度与内容自适应

1. 模型复杂度由题目决定，不以模型数量或算法名称评价质量。
2. 简单模型足以解决问题时，应优先采用结构清楚、可解释、可验证的方法。
3. 使用复杂模型时，必须说明简单方法为何不足、复杂模型新增了什么能力、是否获得可核验增益。
4. 不同题型采用不同数学表达；非优化问题不得强行设置目标函数和 `s.t.`。
5. 不得照搬范文中的模型、变量、公式、图表和章节细节。
6. 题型不适合某项图表或验证时，应使用推导、证明、算例、边界检查或其它等效证据，不得直接删去论证职责。

### 2.3 物理、工程与连续系统问题

处理物理机理、工程设计、微分方程、连续介质、传热传质或数值模拟问题时，应：

1. 从守恒定律、物理定律、几何关系或可靠机理出发，不直接堆叠经验公式；
2. 说明真实场景如何被降维、分层、理想化及忽略哪些次要因素；
3. 完整列出控制方程、初始条件、边界条件、界面条件、耦合条件和定义域；
4. 不能解析求解时，比较候选数值方法的稳定性、精度和适用条件；
5. 写清必要的离散过程、网格或步长、矩阵关系和求解方法，不能只写“调用软件求解”；
6. 使用实测值、基准解、残差、收敛性、守恒量、网格无关性或误差指标进行量化验证；
7. 对关键参数、边界条件和最优方案进行灵敏度、扰动或最优性检查；
8. 在模型评价中回扣简化假设、数值误差、计算可行性和适用边界。

仅凭曲线“看起来接近”不得宣称模型正确，图形比较必须配合数值指标或明确判据。

### 2.4 数据驱动、评价、预测与决策问题

处理统计、评价、分类、预测、机器学习或数据驱动决策问题时，应：

1. 先完成数据审计，再进行特征构造、训练或优化；
2. 按业务含义、数学作用或因果位置解释特征，不把大量原始字段直接堆入模型；
3. 指标必须说明方向性、计算方式、实际含义及其与题目目标的关系；
4. 原则上设置可解释的基准模型，再说明复杂模型相对基准新增的能力和实际增益；
5. 使用与任务匹配的数据划分和评价指标，不得只报告训练集表现；
6. 类别不平衡、时间依赖、空间相关或对象重复时，采用相应验证和防泄漏方案；
7. 集成模型说明基模型、融合方式、权重或投票依据，以及相对单模型的增益；
8. 预测概率、评分或聚类标签必须继续转化为题目要求的排序、分配、额度、类别、路径、策略或方案；
9. 结果表必须直接回答题目，不能让读者自行从中间概率或原始输出推导最终答案；
10. 正文保留核心模型、关键比较和主要结果，超长明细放入附录。

高准确率、高拟合度或高评价指标不能单独证明模型优秀，必须同时说明数据划分、评价口径、泄漏检查、稳健性和适用范围。

### 2.5 多子问题、情景变化与模型继承

当各子问题具有递进关系时，应：

1. 在问题分析中说明各问依赖关系和整体技术路线；
2. 明确后续问题继承了哪些数据、变量、模型、参数和结论；
3. 单独列出新增或修改的目标、约束、参数、情景和输出；
4. 不重复粘贴完全不变的公式，也不得只写“同前”而省略关键修改；
5. 情景变化形成“影响因素—参数或指标变化—模型调整—结果变化”的逻辑链；
6. 每一问都给出独立、可识别的最终答案和验证材料。

### 2.6 真实性、可核验性与全文一致

- 不得编造原始数据、参数来源、计算结果、模型性能、软件输出、文献或检验结论。
- 所有具体数值必须来自题目数据、可核验资料、明确推导或真实运行的程序输出。
- 缺少必要数据时，必须说明缺口，并采用有依据的假设、符号化表达、区间结果或条件性结论。
- 摘要、正文、图表、附录和支撑材料中的模型名称、数值、单位、对象编号和舍入必须一致。
- 每个英文缩写首次出现时使用“中文名称（英文全称，英文缩写）”；摘要与正文分别视为独立文本，首次出现均需解释。
- 不得把流程图、结构图、代码截图或软件界面当作求解结果。

---

## 3. 固定论文结构与章节适配

论文默认使用以下一级结构。题目有几问，就设置几个对应分问；少于四问删除多余分问，多于四问按同样结构顺延。

```markdown
# 题目

## 摘要
关键词：

——摘要结束后强制分页——

## 一、问题重述
### 1.1 问题背景
### 1.2 问题要求

## 二、问题分析
### 2.1 问题一的分析
### 2.2 问题二的分析
### 2.3 问题三的分析
### 2.4 问题四的分析

## 三、模型假设

## 四、符号说明

## 五、模型的建立与求解
### 5.1 问题一模型的建立与求解
#### 5.1.1 问题一模型的分析
#### 5.1.2 问题一模型的建立
#### 5.1.3 问题一模型的求解
#### 5.1.4 问题一结果的分析

### 5.2 问题二模型的建立与求解
#### 5.2.1 问题二模型的分析
#### 5.2.2 问题二模型的建立
#### 5.2.3 问题二模型的求解
#### 5.2.4 问题二结果的分析

### 5.3 问题三模型的建立与求解
#### 5.3.1 问题三模型的分析
#### 5.3.2 问题三模型的建立
#### 5.3.3 问题三模型的求解
#### 5.3.4 问题三结果的分析

### 5.4 问题四模型的建立与求解
#### 5.4.1 问题四模型的分析
#### 5.4.2 问题四模型的建立
#### 5.4.3 问题四模型的求解
#### 5.4.4 问题四结果的分析

## 六、模型的评价与推广
### 6.1 模型的优点
### 6.2 模型的缺点
### 6.3 模型的改进与推广

## 七、参考文献

——参考文献结束后强制分页——

## 附录
### 附录1 问题一所用代码
### 附录2 问题二所用代码
### 附录3 问题三所用代码
### 附录4 问题四所用代码
```

适配规则：

- 一级章节顺序固定；只有比赛官方要求或用户明确要求与本结构冲突时才可调整，并必须保留全部章节职责。
- 子问题数量按题目实际增减；同一问使用多个模型时，可在模型建立或求解下增设更低级标题。
- 不得把第二章“问题分析”与第五章“模型建立”合并。
- 第五章每问的“模型分析”必须保留，负责本问就近数学化、模型引入和与前问的差异说明；不得直接复制第二章。
- 数据预处理、模型检验、敏感性分析或方案说明内容较多时，可在相应分问内单列小节，但不得打乱论文主线。
- 标题、摘要正文和关键词共同独占一页；问题重述从下一页开始。
- 正式提交论文不设目录。
- 附录总体另起一页，本工作区要求每个独立附录或附件单独新起一页。

---

## 4. 页面、字体、字号与段落

### 4.1 页面设置

1. 有官方模板时，严格服从官方纸张、方向、页边距、页眉页脚和页码要求。
2. 官方未规定时使用 A4、纵向、单栏，页边距不小于官方要求。
3. 不得为把摘要塞入一页而缩小到不可读字号、压缩行距或缩窄页边距。
4. 宽表确需横向时，可对单独页面分节设置横向，其余页面保持纵向。
5. `templates/摘要+论文框架.docx` 的横向页面、左右文本框、彩色提示、黄色高亮和虚线边框属于培训展示，不自动继承为正式论文版式。
6. 正式稿必须删除模板提示、批注、修订、占位符和教学文字。

### 4.2 字体硬要求

1. 全部中文字符统一使用宋体，包括题目、标题、正文、摘要、关键词、图题、表题、表内文字、页眉页脚、参考文献和附录。
2. 中文标题强调时使用加粗宋体，不改用黑体、楷体、仿宋或微软雅黑。
3. 全部英文、英文缩写、拉丁字符、阿拉伯数字、希腊字母和数学符号统一使用 Times New Roman 或兼容的 Times 系列数学字体。
4. 数学变量和参数使用 Times 系列斜体；函数名、单位和说明性符号使用正体。
5. 图表内部执行同一中西文字体规则，不保留绘图软件默认字体。
6. 代码块可使用等宽字体，代码外标题和说明仍执行宋体/Times 规则。
7. 不得通过在交付包中嵌入或分发字体文件规避字体要求。

### 4.3 推荐字号与段落

官方或赛区未规定时采用以下默认值：

| 内容 | 字体与字号 | 对齐与段落 |
|---|---|---|
| 论文题目 | 宋体，小二或三号，加粗 | 居中，尽量一行 |
| “摘要”标题 | 宋体，四号，加粗 | 居中 |
| 摘要正文 | 宋体，小四 | 两端对齐，首行缩进 2 字符 |
| 关键词 | 宋体，小四 | 左对齐，“关键词：”加粗 |
| 一级标题 | 宋体，四号，加粗 | 居中 |
| 二级、三级标题 | 宋体，四号，加粗 | 左对齐，不首行缩进 |
| 正文 | 宋体，小四或四号 | 两端对齐，首行缩进 2 字符 |
| 图题、表题 | 宋体，五号 | 居中 |
| 表内文字 | 宋体，五号或小五 | 按列统一 |
| 参考文献 | 宋体，五号 | 悬挂缩进 |

全文段前段后、行距、编号和缩进必须统一。同级标题不得出现字号、粗细或缩进漂移。

---

## 5. 标题、摘要与关键词

### 5.1 论文标题

1. 标题应体现研究对象，并优先体现正文真正起主导作用的核心模型或算法。
2. 宜控制在一行内，不得为追求短而丢失研究对象。
3. 可采用“基于 XXX 模型的 XXX 问题研究”“基于 XXX 模型与 XXX 方法的 XXX 研究”等形式。
4. 当研究对象足够明确时，可采用简洁问题型标题，不必机械使用“基于”。
5. 不得堆砌正文未重点使用的算法名称，不得使用“某问题研究”“数学模型分析”等空泛标题。

### 5.2 摘要页硬要求

1. 标题、摘要正文和关键词必须共同位于摘要专用页，正文另起一页。
2. 摘要控制在一个版面内，使评审仅阅读第一页即可知道研究内容、逐问方法、关键结果和可信度。
3. 不得在摘要中引用图、表、公式或参考文献编号。
4. 不得写程序目录、软件与库版本、运行入口、文件路径、长公式推导或大段背景常识。

### 5.3 摘要固定逻辑

#### 第一段：总体概述

通常使用 2—3 句话、约 3—5 行，包含：

- 问题背景或研究意义；
- 总体建模主线；
- 主要模型或方法类别；
- 解决的核心问题或总体成果。

第一段不得展开详细求解过程，也不得无序罗列模型名称。

#### 各子问题摘要

每个子问题单独成段，依次说明：

1. 本问任务或数学类型；
2. 关键建模思路；
3. 核心模型或方法；
4. 必要的数据与求解方式；
5. 题目要求的关键结果；
6. 结果意义、检验或可信度。

计算型问题必须给出题目要求的核心数值与单位；开放型问题必须给出明确方案、排序、类别、策略或结论。不得只写“得到了较好结果”。

#### 最后一段：总结与升华

有实质内容时，可概括模型体系、主要优点、稳健性、局限、适用范围或推广价值；没有实质信息时可省略，不写空泛套话。

### 5.4 摘要加粗规则

摘要中的**核心模型、关键数值、最终方案和结论性短语必须加粗**。可同时加粗“针对问题一/二/三”等引导语，但不得整段加粗或大量加粗过程细节。

### 5.5 关键词

1. 一般为 4—6 个；
2. 优先覆盖研究对象、研究目的、核心模型、求解算法和验证方法；
3. 使用统一分隔符，推荐中文分号；
4. 不使用“数学建模”“问题分析”“MATLAB”等过宽词作为主要关键词；
5. 缩写作为关键词时，摘要中应已解释全称。

---

## 6. 正文章节写作要求

### 6.1 一、问题重述

#### 6.1.1 问题背景

- 用自己的语言概括真实背景、研究对象和应用意义；
- 删除故事化、宣传性和无关描述；
- 不提前展开模型、算法和结果；
- 原则上控制在 1—3 个自然段。

#### 6.1.2 问题要求

- 将题目各问转化为明确任务；
- 写清输入、条件、约束和输出；
- 对题目要求生成的附件、表格、预测文件等单独注明；
- 不得照抄原题，不得改变题意。

### 6.2 二、问题分析

第二章必须按子问题分别说明：

- 子问题的数学本质；
- 已知量、未知量和主要约束；
- 数据特征和核心困难；
- 与前后问题的关系；
- 拟采用的模型或方法；
- 选择该方法的理由；
- 预期输出和验证路径。

复杂题宜在章末设置总体技术路线或整体建模思路，并可配置流程图。流程图必须与后文章节和实际计算逻辑一致。

### 6.3 第五章各问的“模型分析”

每问的模型分析通常控制在 3—4 行，其职责是：

- 将本问具体数学化；
- 说明准备建立的模型；
- 说明与前问的继承或区别；
- 引出模型建立和求解。

不得直接复制第二章对应内容，不得以“前文已经分析”为由删除。

### 6.4 章节衔接与段落闭环

1. 各节开头说明本节目的及与上一节的关系；
2. 各节结尾给出本节得到的模型、参数、结论或下一步输入；
3. 重要图、表和公式采用“文字引入—对象呈现—结果解释”的闭环；
4. 不得在章节之间突然切换模型、数据口径或符号而无过渡；
5. “综上”“由此”“因此”等连接语必须对应真实推导关系。

### 6.5 三、模型假设

1. 原则上用纯文字编号列举，不放无关图表和复杂公式。
2. 数量按需要确定，常见为 4—7 条，但不得凑数。
3. 假设必须与模型直接相关，并说明物理、业务或统计合理性。
4. 不重复题目已明确给出的事实，不以假设直接消除核心难点。
5. 不写“题目数据真实可靠”“不考虑一切偶然因素”等空泛假设。
6. 对结果影响显著的假设，必须在误差、灵敏度或模型评价中回扣。
7. 数据驱动模型应区分业务假设、统计假设和数据处理规则。

### 6.6 四、符号说明

1. 必须使用三线表；
2. 原则上包含“符号、含义、单位”三列；
3. 无量纲量单位标为“—”；
4. 同一符号全文含义唯一；
5. 集中列示高频核心符号，局部临时变量可在公式附近定义；
6. 向量、矩阵、集合、随机变量与标量的字体规则必须统一；
7. 不得把模型名、软件名和无需解释的常用数学符号大量塞入符号表。

---

## 7. 数据理解与预处理

题目含数据附件时，必须先完成数据审计，再开始模型训练、评价或优化。

### 7.1 数据审计内容

必须核对并在论文中呈现会影响建模与结论的部分：

- 数据来源、文件用途和关键字段；
- 字段类型、单位、样本量、时间范围和空间范围；
- 主键或对象标识；
- 缺失值、重复值、异常值和非法编码；
- 类别分布、数量级和主要统计特征；
- 处理前后样本量；
- 训练、验证、测试或时间切分方式；
- 数据泄漏风险及防范措施。

完整文件清单、脚本路径、随机种子归档和数据审计日志记录在工作区，不要求逐项写入正文。

### 7.2 预处理规则

1. 不得只写“进行了数据预处理”。
2. 删除、填补、截尾、平滑、标准化、编码和聚合必须说明规则和理由。
3. 不得无说明删除异常值；异常值可能代表真实极端状态时，应采用稳健方法或分情形分析。
4. 标准化、降维和特征选择原则上只在训练数据上拟合，再应用于验证/测试数据。
5. 时间序列不得随机打乱导致未来信息泄漏。
6. 同一对象的多条记录不得不当分散到训练集和测试集造成对象泄漏。
7. 所有处理必须与实际代码和结果一致。

### 7.3 可视化预处理

按需要使用最少但有效的图表说明缺失模式、异常值、离散程度、偏态、时间变化、类别不平衡、变量关系或空间分布。不得为每一列机械作图；每张图必须支持一个明确处理决定。

---

## 8. 模型建立的强制写法

### 8.1 总原则：先解释，后数学化

每个子问题必须遵循：

```text
建模思路与关键问题转化
→ 数据处理或机理简化
→ 变量、参数和评价指标定义
→ 局部关系与组成公式推导
→ 完整模型集中汇总
→ 模型输出与题目要求对应
```

不得一进入模型建立就堆放目标函数、约束或大段公式。

### 8.2 问题转化与模型选择

必须说明：

1. 原问题如何抽象为数学问题；
2. 核心对象、主要矛盾和待求量；
3. 保留和忽略了哪些现实因素；
4. 本问属于优化、预测、评价、分类、微分方程、仿真或其它何种任务；
5. 为什么选择当前模型，候选方法为何不作为主模型；
6. 模型输入、输出与题目目标如何对应；
7. 与前面子问题的继承、修改或扩展关系。

### 8.3 变量、参数与指标

按实际需要定义决策变量、状态变量、输入输出变量、模型参数、中间变量、风险收益指标、随机变量、时间/空间/对象索引、单位、定义域和取值范围。

任何符号必须在首次使用前或首次使用处解释；高频符号与符号说明表一致。

### 8.4 局部关系与公式推导

应按模型逻辑分别建立收益、成本、风险、概率、损失、似然、评价指标、守恒关系、状态转移、控制方程、初边值条件及资源或业务约束。

每个局部公式出现前必须说明建立依据，出现后解释各项数学含义和实际意义。不得连续堆放多个公式而无文字说明。

### 8.5 每问的完整模型汇总

完成局部推导后，每个子问题必须集中给出一次可独立阅读的完整模型，包括实际需要的：

- 目标函数或核心方程；
- `s.t.` 约束；
- 初始条件、边界条件和耦合条件；
- 变量范围、定义域或可行域；
- 参数条件；
- 输入与输出。

完整模型之后逐项说明目标、主要条件和输出如何回答题目。除等价变形或求解需要外，汇总后不得新增未纳入汇总的核心关系。

### 8.6 优化模型

必须集中列出决策变量、目标函数、`s.t.` 约束、变量范围、多目标处理方法以及目标与约束的实际意义。

优化模型先列目标函数，下一行以正体 `s.t.` 集中列约束。当同一模型存在三条及以上并列约束时，使用左大括号统括并按主要关系符对齐：

```latex
\begin{aligned}
\min_{\boldsymbol{x}}\quad & f(\boldsymbol{x})\\
\mathrm{s.t.}\quad &
\left\{
\begin{aligned}
g_1(\boldsymbol{x}) &\le 0,\\
g_2(\boldsymbol{x}) &\le 0,\\
h_1(\boldsymbol{x}) &= 0,\\
\boldsymbol{x} &\in \Omega.
\end{aligned}
\right.
\end{aligned}
```

多目标模型必须说明加权和、层次优化、ε-约束、Pareto 前沿或其它方法的理由；不同量纲目标先无量纲化。权重不得无依据指定，关键权重应做稳定性或敏感性检查。

整数、离散或组合决策不得先求连续解后直接四舍五入作为最终答案。取整、修复或启发式后处理后，必须重新检查全部约束、目标值变化和方案可行性。

### 8.7 微分方程与物理模型

必须集中列出控制方程、初始条件、边界条件、界面或耦合条件、定义域、参数物理意义与单位及简化假设。从三维降至二维/一维、连续系统离散化或多层系统耦合时必须说明依据。

### 8.8 统计与机器学习模型

必须集中说明预测目标或标签、特征含义、数据划分、核心预测函数、损失/似然/学习准则、参数估计、超参数选择、评价指标、基准模型和防泄漏措施。

分类模型不得只给准确率；回归模型不得只给拟合曲线；聚类模型不得只给聚类图而无聚类数选择和解释。

### 8.9 评价模型

必须集中说明指标体系、指标方向、标准化、权重计算、综合评价函数、排序或分类规则、权重稳定性和指标冗余检查。每个指标必须解释“为什么影响评价对象”和“数值增大意味着什么”。

### 8.10 算法与仿真模型

必须集中说明输入输出、状态变量、核心规则或迭代公式、关键参数、初始状态、停止条件、重复仿真次数和结果汇总方式。只有当规模可行性影响方法选择或结论时，才讨论运行规模或算法复杂度；不得机械写时间复杂度、空间复杂度。

---

## 9. 模型求解与算法说明

每个子问题的模型求解必须说明：

1. 采用的求解方法、算法或求解器及必要选择依据；
2. 求解基本思路和关键步骤；
3. 对结果有实际影响的搜索范围、步长、网格、容差、样本量、超参数或停止条件；
4. 主要软件、求解器或计算工具名称；
5. 得到的关键数值、方案、预测、分类、评价或仿真结果；
6. 结果是否满足约束、量纲、边界条件和实际常识；
7. 论文中的步骤、公式、参数、图表与实际计算必须一致。

不得为满足形式而虚构迭代过程、搜索范围、随机种子或算法比较。解析求解、直接代入或简单代数计算可简要说明，无需机械拆分步骤。

### 9.1 方法选择与必要比较

当存在两种及以上真正可行的方法，且选择会显著影响精度、稳定性、计算效率、可解释性或可靠性时，应比较与本题有关的指标并说明选择依据。

无需机械比较全部复杂度指标。稳定性、收敛性、网格无关性及不同初值一致性只在相关方法适用且会影响结论时检查。

### 9.2 求解步骤

复杂算法或多阶段计算宜使用编号步骤，内容与实际过程一致，例如：

1. 准备输入数据或初始参数；
2. 完成必要预处理或变量计算；
3. 建立待求方程、目标函数或迭代关系；
4. 执行核心计算或调用求解器；
5. 检查收敛、约束和边界条件；
6. 输出并解释关键结果。

步骤数量按实际过程确定，流程图只能辅助，不能替代文字、公式和结果。

### 9.3 正文与工作区的工程边界

正文只说明影响模型和结论的科学设置。以下内容默认记录在工作区，不写入论文正文：

- 操作系统、硬件、软件和依赖库的完整版本表；
- 环境安装命令、运行入口、命令行和目录树；
- 输入输出文件路径、日志位置、缓存和完整默认参数；
- 完整复现手册；
- 与题目无关的时间复杂度、空间复杂度和工程性能明细。

若某一版本差异、随机设置、硬件精度或规模限制会实质改变结果，必须在论文中说明其科学影响，而不是只给工程清单。

---

## 10. 结果呈现、分析与验证

### 10.1 每问最低要求

每个子问题必须：

1. 给出题目要求的主要结果；
2. 用文字直接回答本问；
3. 解释结果的实际含义；
4. 提供充分且适用的分析或验证；
5. 说明结果的适用条件或主要限制。

原则上，每问从结果分析、误差分析、模型检验、灵敏度分析或其它等效证据中选择至少两项；确有题型原因不适用时，必须用推导、证明、基准算例、边界检查、约束检查等补足论证，不得降低可信度。

### 10.2 可选分析与验证

- 结果分析；
- 误差或残差分析；
- 模型检验；
- 灵敏度和稳健性分析；
- 基准模型对比；
- 消融实验；
- 交叉验证；
- 收敛性或网格无关性分析；
- 守恒量检查；
- 极端情形或边界检查；
- 约束满足性检查；
- 现实案例、典型样本或抽样稳定性验证。

没有真实值时不得强行计算“误差率”；没有关键可扰动参数时不得机械做灵敏度分析；纯理论问题可用推导、证明或算例替代。

### 10.3 先给答案，再展开证据

每问的结果分析应先用一段话明确给出最终答案，再用表格、图形、指标、推导或案例解释依据。不得让评审阅读数页后仍无法确定结论。

结果表原则上包含对象编号或类别、核心输出、单位、必要误差或状态、最终决策或结论。正文展示代表性结果和关键汇总，超长明细放附录，并在正文指出位置。

### 10.4 结果可信度与结论强度

验证结论必须给出判据、过程、数值/图表/统计量、明确判断及仍存在的不确定性。

“准确、稳定、最优、显著、优于”等词必须有相应证据。禁止仅写“由图可知模型准确”“结果符合实际”“效果很好”。

### 10.5 典型案例与解释

适合时选取最优、最差、中间、边界、异常或不同类别代表样本，说明模型为何产生对应结果，以增强可解释性和实际可操作性。

---

## 11. 图表选择、位置与排版

### 11.1 图表总原则

1. 每张图表必须服务一个明确论点，不为凑数量或种类而生成。
2. 不机械要求每问同时有图、数据和表格，但每问必须用足够的数值、图表、推导或算例支撑结果。
3. 图、表和文字不得重复展示完全相同的信息。
4. 图表必须遵循“前文引入并说明目的—呈现图表—后文解释主要发现和作用”。
5. 图表靠近首次引用位置，图题、表题不得与主体分离，标题不得孤悬页末。
6. 图表中的数据口径、筛选范围、单位和舍入必须与正文一致。
7. 同一结果图不得在多个章节重复出现。

### 11.2 图表位置

模型建立部分适合放置解释“模型如何工作”的图：

- 总体技术路线图；
- 建模流程图；
- 模型结构图；
- 算法原理图；
- 物理结构示意图；
- 差分节点图或网格剖分图；
- 指标体系图；
- 神经网络或集成模型结构图。

模型求解、结果分析或检验部分适合放置展示“模型算出什么”的图：

- 箱线图、散点图、折线图、柱状图和直方图；
- 拟合曲线、真实值—预测值图和残差图；
- ROC、PR 曲线和混淆矩阵；
- 热力图、空间分布图、等高线图或云图；
- 灵敏度曲线、方案对比图和 Pareto 前沿；
- 收敛过程图和误差变化图。

### 11.3 图表选择指南

| 论证目的或数据结构 | 推荐图表 |
|---|---|
| 缺失模式 | 缺失矩阵或缺失比例表 |
| 异常值、离散程度 | 箱线图 |
| 时间或参数变化趋势 | 折线图 |
| 两变量关系 | 散点图及必要拟合线 |
| 类别比较 | 柱状图或点图 |
| 数据分布 | 直方图、核密度图或经验分布图 |
| 分类效果 | 混淆矩阵、ROC 或 PR 曲线 |
| 回归误差 | 残差图、真实值—预测值图 |
| 矩阵关系 | 热力图 |
| 空间分布 | 地图、等高线图或云图 |
| 算法逻辑 | 流程图或结构图 |
| 多方案排名 | 三线表配合柱状图或点图 |
| 参数敏感性 | 参数—结果曲线 |
| 多目标权衡 | Pareto 前沿 |
| 收敛过程 | 目标值—迭代次数曲线 |

不推荐无实际意义的三维柱状图、爆炸饼图、雷达图堆叠、渐变装饰背景和过度艺术化图形。

### 11.4 图的排版

1. 图题置于图下，统一为“图 x  标题”；
2. 图必须先在正文中引用，再出现；
3. 坐标轴名称、单位、图例、刻度和编号完整；
4. 同类图保持尺寸、字体、线宽和风格一致；
5. 图中文字在最终打印尺寸下必须清晰可读；
6. 中文使用宋体，英文字符和数字使用 Times New Roman；
7. 不得截取 MATLAB、Python、Excel、SPSS 等软件界面作为正式结果图；
8. 不得手工修改图中数据、坐标或标签以迎合结论；
9. 图必须由最终数据或程序生成，并与正文结果一致。

### 11.5 表格排版

1. 表题置于表上，统一为“表 x  标题”；
2. 表格优先使用三线表，不使用无意义竖线；
3. 表头写清变量或指标及单位；
4. 同列数值的小数位、有效数字和对齐方式统一；
5. 数值过多时展示代表行并用省略号说明，完整表放附录；
6. 不得把数据表截图插入；
7. 表后必须解释主要发现，而不是逐行朗读；
8. 跨页表必须重复表头，避免表题与表体分离；
9. 列宽应按内容分配，避免等宽导致长文本拥挤或短数值列浪费；
10. 单元格留有适当内边距，不使用会截断文字的固定行高。

---

## 12. 公式、符号、单位与有效数字

### 12.1 公式排版

1. 重要公式单独居中，编号右对齐；
2. 全文采用统一编号方式，可连续编号或按章编号；
3. 正文引用使用“式（x）”，不使用含义不清的“上式”“下式”；
4. 多行公式按等号或主要关系符对齐；
5. 一个完整公式组尽量不跨页；
6. 公式标点符合所在句子的语法；
7. 公式不得以图片或截图插入；
8. 公式前说明建立依据，公式后解释变量、条件和实际含义。

### 12.2 多条方程与条件的左大括号规则

1. 同一模型中存在三条及以上并列约束、控制方程、初边值条件、耦合条件、状态转移或分段规则时，使用左大括号统括；
2. 组内按等号、不等号或主要关系符纵向对齐；
3. `s.t.` 仅用于优化模型，置于约束组左侧并使用正体；
4. 微分方程、联立方程和分段关系只使用大括号，不机械添加 `s.t.`；
5. 一条或两条简单关系可直接对齐；
6. 公式组过长时按物理含义或约束类别拆分并说明各组作用；
7. 一个完整公式组原则上使用一个编号，需要独立引用内部条件时使用子编号；
8. 不得为追求大括号形式把不同模型强行合并。

### 12.3 正斜体规则

1. 标量变量、参数、未知量、一般函数自变量、表示数学量的拉丁字母和希腊字母使用 Times 系列斜体；
2. 向量、矩阵使用统一的 Times 系列粗斜体或粗体；
3. 下标为变量索引时用斜体，下标为英文缩写、中文说明或固定含义时用正体；
4. `sin`、`cos`、`ln`、`exp`、`max`、`min`、`argmin`、`argmax`、`s.t.`、微分符号、单位和转置符号使用正体；
5. 数字使用 Times New Roman 正体；
6. 公式中的中文说明使用宋体；
7. 不得在同一论文中混用多套数学字体。

### 12.4 单位与量纲

1. 重要变量首次出现时给出单位；
2. 表头和坐标轴必须标单位；
3. 单位使用规范符号，不随意中英文混写；
4. 同一物理量全文采用统一单位；
5. 公式相加减项必须量纲一致；
6. 不同量纲项组成目标函数时，必须说明归一化或尺度处理。

### 12.5 有效数字

1. 结果精度由原始数据精度、模型误差和实际应用共同决定；
2. 不得输出虚假高精度；
3. 同类结果采用统一小数位；
4. 整数型决策不得保留无意义小数；
5. 百分数与小数表达全文统一；
6. 区分“百分比变化”和“百分点变化”；
7. 摘要、正文、图表、附录和代码输出的数值、单位和舍入必须一致。

---

## 13. 模型评价、改进与推广

### 13.1 模型优点

必须结合实际证据说明，可从机理解释性、数据适应性、稳定性、精度、求解效率、特殊结构利用、决策可操作性、稳健性或规模扩展性中选择。每个优点应指出对应模型结构、实验结果或检验证据。

### 13.2 模型缺点

如实说明假设限制、数据限制、参数依赖、未考虑因素、局部最优风险、泛化能力、数值误差和实际应用条件。不得把缺点写成推翻全部论文的致命错误，也不得回避真实局限。

### 13.3 模型改进与推广

必须说明：

- 可推广到哪些对象、地区、行业或尺度；
- 推广时哪些变量、约束和参数需调整；
- 需要补充什么数据；
- 如何扩展到动态、多目标、随机或鲁棒场景；
- 推广后的验证方式。

不得只写“模型具有良好的推广性”。

---

## 14. 参考文献与引用

### 14.1 数量与组成

参考文献原则上不少于 **6 篇**。以下两本基础教材必须列入参考文献，并在正文介绍相应建模方法、算法或通用理论时实际引用：

```text
[1] 姜启源, 谢金星, 叶俊. 数学模型（第五版）[M]. 北京: 高等教育出版社, 2018.
[2] 司守奎. 数学建模算法与应用[M]. 北京: 国防工业出版社, 2011.
```

其余至少 **4 篇**应与题目背景、核心模型、算法、参数来源或验证方法直接相关。

这是本工作区的论文质量门禁，不冒充全国组委会统一规定。不得为了满足数量加入无关文献；若未能检索、核验并实际使用足够文献，应阻止发布而不是虚构或凑数。

### 14.2 文献质量

1. 所有文献必须真实、可检索并已实际打开核验；
2. 正文引用与文后编号一一对应；
3. 不得加入正文未引用的文献；
4. 专业背景、关键参数和算法优先引用原始论文、权威教材、标准、官方文档或高质量综述；
5. 不得用搜索结果摘要、不可靠网页或二手博客支撑关键专业依据；
6. 引用算法时优先引用原始或权威来源；
7. 文献格式全文统一；
8. 在线数据和网页资料写明足够的来源信息及访问日期。

### 14.3 正文引用

- 引用放在对应事实、方法、公式或参数附近；
- 不在段末堆放多个无法判断对应关系的文献编号；
- 自行推导的公式不伪装成文献结论；
- 两本指定教材必须在正文实际方法介绍处引用，不能只出现在文末；
- 所有公开来源、数据集、他人成果和按官方要求披露的 AI 工具均应规范标注。

文献检索过程、DOI/URL 核验和文献台账记录在工作区；论文呈现规范的正文引用和参考文献表。

---

## 15. AI 工具使用、附录与匿名性

### 15.1 AI 工具使用

严格服从当届官方规定。按 2025 年试行规定：

- 未使用 AI 工具时，在参考文献后按官方措辞声明；
- 使用 AI 工具生成或辅助的内容，在正文相应位置标注；
- 在参考文献中列出工具名称、版本/型号、机构和使用日期；
- 支撑材料包含官方要求的 `AI 工具使用详情.pdf`。

完整提示词、回复、采纳和人工修改过程记录在工作区 AI 台账中，不在正文堆砌。

### 15.2 附录结构与内容

1. 附录总体另起一页，每个独立附录或附件单独新起一页。
2. 按题目分问或实际程序模块组织，子问题数量按题目增减。
3. 按当届官方规定列出支撑材料文件清单和建模所用全部完整、可运行的源程序。
4. 没有使用程序或没有支撑材料时，使用官方要求的说明。
5. 可放置正文不宜展开的长表、补充推导、算法伪代码、参数表和补充结果。
6. 附录代码与正文图、表和结果必须对应，不得包含无关测试或调试内容。
7. 附录和代码不得包含身份信息、个人绝对路径或隐私内容。
8. 每个代码附录必须说明其解决的子问题以及与正文图、表、公式或结果的对应关系；必要时同时给出算法伪代码、重要参数、补充结果、过长表格或必要推导。

环境安装、依赖锁定、运行命令、目录说明和完整复现手册放在工作区与支撑材料说明中，不写成正文式章节。

### 15.3 匿名性

论文、附录、代码注释、文件名、图片属性、文档元数据和支撑材料均不得出现队员、学校、指导教师、赛区、用户名或可反推出身份的本机路径。

---

## 16. 全文一致性检查

必须检查：

- 摘要与正文模型名称一致；
- 摘要关键数值与正文一致；
- 正文与真实计算输出一致；
- 图表与正文数值一致；
- 公式符号与符号表一致；
- 单位和小数位一致；
- 题目对象编号一致；
- 参考文献编号一致；
- 附录文件清单与实际支撑材料一致；
- 后续问题继承的参数、变量和结论与前问一致；
- 模型评价中的优缺点与实际验证结果一致。

所有摘要关键数值和最终决策都必须能在工作区证据索引中定位，但证据索引本身不作为论文正文章节。

---

## 17. 严格禁止事项

不得：

1. 照抄题目、模板、范文或他人论文；
2. 编造数据、运行结果、模型指标、参考文献或软件输出；
3. 未运行代码却声称“计算得到”；
4. 用代码、截图、流程图或软件界面替代模型说明；
5. 为显得高级堆砌算法或对每问机械使用同一模型；
6. 对非优化问题强行写目标函数和 `s.t.`；
7. 未解释问题转化、变量和局部关系就直接给出完整模型；
8. 完整模型汇总后继续新增未纳入汇总的核心关系；
9. 连续解取整后不重新检查约束和可行性；
10. 没有验证证据却宣称模型准确、稳定、最优或优越；
11. 只报告训练集指标或造成数据泄漏；
12. 无说明删除异常值、缺失值或样本；
13. 为凑形式强制每问同时放图、数据和表格；
14. 使用无意义三维图、爆炸饼图和过度装饰图；
15. 使用公式、表格、代码或软件界面截图；
16. 让摘要、正文、图表、附录和支撑材料出现不同数值；
17. 使用虚假高精度；
18. 在正式稿保留彩色教学提示、高亮、批注、修订和占位符；
19. 在正文堆放环境版本、库版本、运行入口、目录树、日志或无关复杂度分析；
20. 加入未实际引用、未核验或与题目无关的文献；
21. 两本指定教材只列不引；
22. 交付前未逐页检查最终 PDF 和字体属性。

---

## 18. 论文写作工作流

### 阶段 1：题目拆解

- 提取背景、数据、条件、约束和各问输出；
- 标记各问继承关系；
- 标记官方要求生成的文件和附件。

### 阶段 2：数据与机理理解

- 数据题完成数据审计、预处理和泄漏检查；
- 物理题完成机理、维度、边界和简化分析；
- 只把影响模型和结论的内容写入论文。

### 阶段 3：模型方案设计

- 确定每问数学类型；
- 设计变量、参数、指标和局部关系；
- 选择基准与主模型；
- 规划完整模型汇总形式；
- 设计至少两类适用分析或等效验证。

### 阶段 4：模型求解与结果确认

- 使用真实运行或明确推导得到结果；
- 检查约束、量纲、边界和常识；
- 生成正式图表、指标和验证材料；
- 未确认的数值不得进入论文。

环境、运行入口、日志和证据登记按工作区规范执行，不写入正文。

### 阶段 5：正文写作

按固定目录完成问题重述、问题分析、假设、符号、各问模型分析/建立/求解/结果分析、模型评价、参考文献和附录。

写作顺序可先正文后摘要；最终摘要必须从已确认正文结果反向提炼。

### 阶段 6：排版与一致性检查

- 应用字体和标题样式；
- 统一公式、图表和编号；
- 检查分页、数值、单位、引用和附录；
- 生成最终 PDF 并逐页检查；
- 检查 PDF 字体属性和匿名性。

### 阶段 7：独立审稿

从评审视角检查每问是否被直接回答、模型为何合理、结果从何而来、验证是否充分、图表是否必要、摘要是否与正文一致、引用是否真实、是否存在夸大结论。

未通过任一硬性检查不得发布。

---

## 19. 最终交付前强制检查表

### 19.1 结构、分页与摘要

- [ ] 一级章节顺序符合固定框架，子问题数量与题目一致。
- [ ] 标题、摘要正文和关键词共同独占一页，正文从下一页开始。
- [ ] 正式提交稿无目录，正文页数符合官方要求。
- [ ] 每个独立附录或附件单独新起一页。
- [ ] 第二章问题分析和第五章分问模型分析均保留且不重复。
- [ ] 摘要逐问给出任务、模型、求解和关键结果。
- [ ] 计算型问题的关键数值与单位已写入摘要。
- [ ] 摘要中的核心模型、关键数值、最终方案和结论性短语已加粗。
- [ ] 关键词为 4—6 个且具有检索意义。

### 19.2 数据、模型与求解

- [ ] 数据来源、字段、单位、样本量和关键处理已说明。
- [ ] 缺失、重复、异常、数据划分和泄漏防范有依据。
- [ ] 模型选择有理由，复杂模型相对基准有必要性说明。
- [ ] 模型建立遵循“问题转化—变量指标—局部公式—完整模型汇总”。
- [ ] 每问都有集中列示的完整模型。
- [ ] 优化模型先列目标函数，下一行以 `s.t.` 集中列约束。
- [ ] 三条及以上并列条件已用左大括号统括并对齐。
- [ ] 连续解取整或后处理后已重新检查可行性。
- [ ] 求解思路、关键步骤和影响结果的参数明确。
- [ ] 正文未混入环境、版本、入口、路径、日志和复现手册。

### 19.3 结果、验证与图表

- [ ] 每问先直接回答题目，再展开证据和解释。
- [ ] 原则上每问有至少两项适用分析或等效验证。
- [ ] 所有验证有判据、数值/图表和明确结论。
- [ ] 图表类型与论证目的匹配，位置符合模型建立/结果分析职责。
- [ ] 图题在下、表题在上，图表先引用后出现并有后文解释。
- [ ] 图表编号、坐标、单位、图例和数据口径完整。
- [ ] 表格优先为三线表，小数位、有效数字和对齐统一。
- [ ] 未使用公式、表格、代码或软件界面截图。

### 19.4 公式、字体与排版

- [ ] 重要公式居中、编号右置并使用“式（x）”引用。
- [ ] 公式均有前文依据、后文解释且量纲合理。
- [ ] 全部中文为宋体或加粗宋体。
- [ ] 英文字符、数字和数学符号为 Times New Roman 或统一 Times 数学字体。
- [ ] 数学变量、参数为 Times 系列斜体，函数名和单位为正体。
- [ ] 图表内部字体与正文一致。
- [ ] 正式稿已删除提示、高亮、批注、修订和占位符。

### 19.5 文献、附录与合规

- [ ] 参考文献原则上不少于 6 篇。
- [ ] 两本指定基础教材已列入并在正文实际引用。
- [ ] 其余至少 4 篇文献与题目背景、模型、算法、参数或验证直接相关。
- [ ] 所有文献真实、已核验、正文有引用且格式统一。
- [ ] 附录包含当届官方要求的支撑材料清单和完整可运行代码。
- [ ] 各代码附录已说明所解决问题及与正文图、表、公式或结果的对应关系。
- [ ] AI 使用声明、正文标注、工具引用和详情文件符合当届规定。
- [ ] 论文、代码、文件名和元数据均通过匿名性检查。
- [ ] 摘要、正文、图表、附录、代码和支撑材料结果一致。
- [ ] 最终 PDF 已逐页检查，无溢出、乱码、缺图、断式或字体异常。
- [ ] 已在提交前重新核对全国组委会和赛区最新规则。

---

## 20. Agent 执行摘要

```text
生成数学建模论文时，必须保持完整论文质量，不得以“精简规范”或“工程职责迁移”为由删除论文结构、模型推导、求解、结果、验证、图表、文献和排版要求。

论文使用固定主结构：题目；摘要与关键词；分页；问题重述；问题分析；模型假设；符号说明；各问模型分析、建立、求解和结果分析；模型评价与推广；参考文献；分页；附录。子问题数量按题目增减。

摘要逐问写任务、思路、模型、求解、关键结果和可信度；关键模型、数值、方案与结论加粗。每问集中汇总完整模型；优化模型先列目标函数，下一行以 s.t. 集中列约束。每问原则上至少两项适用分析或等效验证。

图表必须按论证目的选择，放在模型建立或结果分析的合适位置；图题在下、表题在上，先引用后出现并有解释。公式、符号、单位、有效数字和中西文字体必须统一。

参考文献原则上不少于 6 篇，必须包括并实际引用《数学模型（第五版）》和《数学建模算法与应用》，其余至少 4 篇与题目直接相关。不得凑数、虚构或引用未核验资料。

工程环境、依赖版本、运行入口、目录树、日志、证据台账和完整复现手册写入工作区规范指定位置，不进入正文；只有直接影响模型和结论的科学设置写入论文。

发布前逐项检查内容、公式、图表、文献、附录、字体、分页、匿名性和全文一致性，并逐页渲染检查最终 PDF 及字体属性。任何硬性项未通过不得发布。
```
